# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
nul
Thumbs.db
desktop.ini
tmp_*

# IDE / editor
.cursor
.claude
.vscode
.idea

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# MCP config (contains API keys)
.mcp.json

# cloudflare
.open-next
.wrangler

# typescript
*.tsbuildinfo
next-env.d.ts

.env*.local

# One-time rotation scripts
scripts/rotate-encryption-key.ts

# Benchmarking (internal analysis — not for sharing)
docs/benchmark/
docs/benchmarking-*.md
docs/benchmarking-*.html

# Strategy & competitive intelligence (internal — not for sharing)
docs/strategy-private/

# VC application materials (contains personal info)
docs/vc/Primer/

# Design captures & tools (internal dev artifacts)
docs/captures/
docs/tools/

# DB upload SQL files (local-only seed data — not for sharing)
docs/db-uploads/

# Playwright MCP (local tool config)
.playwright-mcp/

# Local design references
*.jpeg
*.jpg

# packages 워크스페이스 산출물 (mcp-server 등)
packages/*/node_modules/
packages/*/dist/

# --- Sensitive / internal — NEVER commit to public repo (2026-06-21) ---
docs/redteam/
railway-*.png
railway-*.md
mcp-report/
linkmap-*.png
docs/planning/feature-ideas-2026-04.html
linkmap/
public/Untitled
result.txt
template-build-results.txt
