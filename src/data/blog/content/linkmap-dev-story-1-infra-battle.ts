export const content = `> **KEY:** AI는 코드를 짜주지만 환경변수 등록, 배포 설정, OAuth 콘솔 설정은 사람이 직접 해야 합니다. 비개발자가 바이브 코딩에서 가장 많이 막히는 건 코드가 아니라 이 인프라 설정입니다.

## 저는 개발자가 아닙니다

코스피 상장사에서 ERP 시스템을 운영하는 담당자입니다. 그런 제가 [Claude Code](https://claude.ai/code)와 [Supabase](https://supabase.com)를 써서 Linkmap이라는 웹서비스를 만들었습니다.

처음 이름은 ReadTree였습니다. 책 읽기 기록을 트리 구조로 시각화하는 서비스였는데, 만들면서 생긴 불편함들 — 외부 서비스 연결이 어렵고, 환경변수가 어디에 있는지 모르겠고, 배포할 때마다 뭔가가 빠지는 느낌 — 을 해결하려다 Linkmap이 됐습니다.

이 글은 기술 튜토리얼이 아닙니다. 비개발자가 바이브 코딩으로 서비스를 만들면서 실제로 부딪힌 고민들, 특히 코드보다 인프라 설정에서 막힌 이야기입니다.

> **INFO:** 바이브 코딩(Vibe Coding)은 AI에게 자연어로 코드 생성을 지시하는 개발 방식입니다. Cursor, Claude Code 같은 AI 코딩 도구를 활용해 코딩 경험 없이도 서비스를 만들 수 있습니다.

## 배포 환경 3번 이전 — 환경변수 매번 리셋

처음엔 Firebase를 썼습니다. AI가 추천해줬고, 문서가 많았습니다. 한 달쯤 후에 Supabase로 옮겼습니다. PostgreSQL이 더 친숙하고, RLS 보안 모델이 ERP의 권한 체계와 비슷하게 느껴졌습니다.

그다음은 배포 플랫폼이었습니다. Vercel에서 시작해서, 트래픽 비용 걱정에 Cloudflare Pages로 옮겼습니다.

배포 환경을 바꿀 때마다 같은 일이 반복됐습니다.

\`\`\`
1. 새 플랫폼에 프로젝트 연결
2. 환경변수 전부 다시 입력
3. 배포
4. 안 됨
5. 어떤 환경변수가 빠졌나 찾기
6. 찾아서 등록
7. 다시 배포
8. 또 안 됨
9. 반복
\`\`\`

로컬에서 잘 되던 기능이 배포 후 조용히 죽어있었습니다. 에러 메시지가 없는 경우가 더 무서웠습니다. 코드를 수정하지 않았는데 갑자기 기능이 안 됐습니다. 대부분은 환경변수 문제였습니다.

> **WARNING:** 배포 플랫폼을 바꿀 때 환경변수를 그냥 복사하면 안 됩니다. 플랫폼마다 환경변수 형식이나 범위(모든 환경 vs Preview vs Production)가 다릅니다. 배포 가이드에서 Vercel과 Cloudflare의 환경변수 등록 방법 차이를 확인하세요.

## OAuth — 코드보다 콘솔 설정이 3배 걸렸다

Linkmap에 구글 로그인을 추가하기로 했습니다. AI에게 물어봤더니 코드는 30분 만에 나왔습니다. 하지만 실제로 동작하기까지는 2시간이 걸렸습니다. 코드는 30분이었고, 콘솔 설정은 3시간이었습니다.

Google Cloud Console에서 OAuth 클라이언트를 만들고, 승인된 리디렉션 URI를 등록하고, Supabase Auth 설정에 Client ID와 Secret을 넣고, Supabase 프로젝트 URL을 Google에 다시 등록하고. 이 순서를 틀리거나 하나라도 빠뜨리면 "redirect_uri_mismatch" 에러가 납니다.

카카오 로그인은 더 복잡했습니다. 카카오 개발자 콘솔 → 앱 생성 → 카카오 로그인 활성화 → Redirect URI 등록 → REST API 키 복사 → Supabase OIDC 설정. 각 단계에서 어떤 키를 어디에 붙여넣는지 처음 해보는 사람은 알 수 없습니다.

코드는 30분이었고, 콘솔 설정은 하루 반이었습니다.

| 작업 | 소요 시간 |
|------|---------|
| AI로 코드 생성 | 30분 |
| Google Cloud Console 설정 | 2시간 |
| Supabase Auth 연동 | 1시간 |
| Redirect URI 오류 해결 | 3시간 |
| 카카오 로그인 설정 | 반나절 |

---

## 환경변수 누락 — 에러 없이 조용히 멈추는 서비스

가장 까다로웠던 건 에러 메시지가 없는 실패였습니다.

환경변수가 빠지면 두 가지 패턴이 있습니다. 하나는 명확한 에러가 납니다. \`NEXT_PUBLIC_SUPABASE_URL is not defined\` 같은 메시지. 이건 찾기 쉽습니다.

다른 하나는 조용히 기능이 꺼집니다. 버튼을 눌러도 반응이 없거나, 데이터가 저장됐는데 화면에 안 보이거나, 로그인 후 바로 로그아웃됩니다. 코드에는 아무 문제가 없습니다. 환경변수가 \`undefined\`로 들어가서 API 호출 자체가 무시됩니다.

이 패턴을 파악하기까지 몇 달이 걸렸습니다. 지금은 기능이 이상하게 느껴지면 제일 먼저 환경변수 등록 상태를 확인합니다.

[Linkmap](https://www.linkmap.biz)을 만들면서 이 경험을 바탕으로 환경변수 관리 기능을 설계했습니다. 어떤 서비스에 어떤 환경변수가 필요한지 한눈에 보여주고, 누락된 값을 알려주는 것. [환경변수 완전 정복 가이드](/guides/env)에서 이 개념의 기초를 다룹니다.

## 이 시리즈에서 다루는 것들

이 개발기는 3편으로 이뤄져 있습니다.

| 편 | 주제 |
|----|------|
| **1편 (지금)** | 배포, OAuth, 환경변수 — 코드 외 인프라와의 싸움 |
| [2편](/blog/linkmap-dev-story-2-ai-as-teammate) | Supabase 설계, RLS 보안, 컨텍스트 한계, PRD |
| [3편](/blog/linkmap-dev-story-3-community-and-next) | 커뮤니티, Claude Code 전환, 서비스 연결의 본질 |

기술 스택을 설명하는 글이 아닙니다. 비개발자가 실제로 서비스를 만들면서 어디서 막히고, 어떻게 돌파했는지의 기록입니다. 비슷한 경험을 하고 있는 분들께 조금이라도 도움이 됐으면 합니다.

[Linkmap 서비스 카탈로그](https://www.linkmap.biz/services)에서 제가 연결한 서비스들이 어떻게 구성되어 있는지 볼 수 있습니다. [무료로 시작하기](https://www.linkmap.biz/signup)로 직접 서비스 맵을 만들어보세요.

> **TRY:** 배포 환경 설정이 막막하다면 [Linkmap 무료 가입](https://www.linkmap.biz/signup) 후 서비스 연결 지도를 먼저 그려보세요. 어떤 서비스가 어떤 환경변수를 필요로 하는지 정리하면 배포 체크리스트로 활용할 수 있습니다.

---

*OAuth 설정 전체 과정은 [인증 구현 가이드](/guides/auth)에서, 이 시리즈의 다음 이야기는 [ERP 담당자의 Linkmap 개발기 ② — AI를 팀원으로 만드는 법](/blog/linkmap-dev-story-2-ai-as-teammate)에서 이어집니다.*`;
