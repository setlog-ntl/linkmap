export const content = `> **KEY:** 바이브코딩의 진짜 어려운 부분은 만드는 것이 아니라 **유지하는 것**입니다. AI가 만든 코드를 이해하지 못해도 관리할 수 있는 실전 전략 5가지를 정리합니다.

## 배포 후 현실

사이트를 배포하고 링크를 공유한 지 일주일. 이런 일이 생깁니다:

- "어제까지 잘 되던 페이지가 갑자기 에러가 나요"
- "새 기능을 추가했더니 기존 기능이 망가졌어요"
- "npm 패키지 업데이트하라는 알림이 자꾸 와요"
- "Supabase에서 보안 경고 메일이 왔는데 뭘 해야 하는지 모르겠어요"

이것이 **유지보수**입니다. 코딩 경험이 있는 개발자도 힘든 일인데, 바이브코더에게는 더 막막하게 느껴집니다. 하지만 몇 가지 원칙만 지키면 충분히 관리할 수 있습니다.

## 전략 1 — Git을 보험으로 사용하기

### "일단 커밋, 자주 커밋"

Git은 코드의 타임머신입니다. **무언가 잘 동작할 때마다** 저장(커밋)해두면, 나중에 문제가 생겼을 때 그 시점으로 돌아갈 수 있습니다.

\`\`\`bash
# 잘 동작하는 상태를 저장
git add .
git commit -m "로그인 기능 정상 동작"

# ... AI에게 새 기능을 추가하라고 시킴 ...
# ... 기존 기능이 망가짐 ...

# 이전 상태로 되돌리기
git checkout -- .
\`\`\`

### 커밋 메시지 규칙

AI에게 어떤 작업을 시킬 때마다 **작업 전에** 커밋하세요:

\`\`\`bash
git add .
git commit -m "결제 기능 추가 전 백업"
\`\`\`

이렇게 하면 AI가 코드를 망쳐도 언제든 되돌릴 수 있습니다. Git 사용법은 [Git 기초 가이드](/blog/git-basics-for-vibe-coders)에서 자세히 다룹니다.

## 전략 2 — AI에게 유지보수도 시키기

Cursor의 가장 큰 장점은 **기존 코드를 읽고 설명할 수 있다**는 점입니다.

### 에러 발생 시

\`\`\`
이 에러를 해결해줘:
[에러 메시지 전체 복사]

단, 다른 기능에 영향을 주지 않도록 최소한의 변경만 해줘.
\`\`\`

**"최소한의 변경"**이 핵심입니다. AI는 에러를 고치면서 관련 없는 코드까지 리팩토링하는 경향이 있습니다.

### 코드 이해하기

\`\`\`
@app/api/login/route.ts 이 파일이 무슨 역할인지
비개발자도 이해할 수 있게 설명해줘.
\`\`\`

\`\`\`
이 프로젝트의 전체 구조를 설명해줘.
어떤 파일이 어떤 역할인지, 데이터가 어떻게 흐르는지
간단한 다이어그램으로 보여줘.
\`\`\`

## 전략 3 — 의존성 업데이트 관리

### npm 패키지 알림

GitHub에서 "Dependabot" 알림이 오면 당황할 수 있습니다. 이건 사용하는 라이브러리에 보안 취약점이 발견됐다는 뜻입니다.

**대응 방법:**

\`\`\`bash
# 현재 상태 백업
git add .
git commit -m "의존성 업데이트 전 백업"

# 보안 패치 적용
npm audit fix

# 사이트가 정상 동작하는지 확인
npm run dev
\`\`\`

정상 동작하면 커밋 후 푸시. 문제가 생기면 \`git checkout -- .\`로 되돌리세요.

### 메이저 버전 업데이트는 하지 마세요

\`\`\`bash
# 이건 하지 마세요 (위험)
npm update --force
\`\`\`

메이저 버전 업데이트(예: v2 → v3)는 코드 호환성을 깨뜨릴 수 있습니다. \`npm audit fix\`로 보안 패치만 적용하는 것이 안전합니다.

> **WARNING:** \`npm audit fix --force\`는 메이저 버전까지 강제 업데이트합니다. \`--force\` 없이 \`npm audit fix\`만 실행하세요.

## 전략 4 — 환경변수 관리

프로젝트가 커지면 환경변수도 늘어납니다. Supabase URL, API 키, Stripe 키...

### 자주 발생하는 문제

- "로컬에서는 되는데 배포하면 안 돼요" → 배포 환경에 환경변수를 설정하지 않음
- "어떤 키가 어디에 쓰이는지 모르겠어요" → 관리 부재
- "Supabase 키를 실수로 GitHub에 올렸어요" → 즉시 키 재발급 필요

### 관리 방법

1. **로컬**: \`.env.local\` 파일에 저장 (반드시 \`.gitignore\`에 포함)
2. **배포**: Vercel/Cloudflare 대시보드의 환경변수 설정에 등록
3. **시각화**: [Linkmap 서비스맵](https://www.linkmap.biz)에서 어떤 서비스에 어떤 키가 연결되어 있는지 관리

자세한 내용은 [환경변수 관리 가이드](/guides/env)를 참고하세요.

## 전략 5 — 모니터링 최소 세팅

### 무료 모니터링 3가지

**1. Vercel Analytics (무료)**
Vercel 대시보드에서 활성화하면 방문자 수, 페이지 성능을 추적할 수 있습니다.

**2. Uptime Robot (무료)**
[UptimeRobot](https://uptimerobot.com)에 내 URL을 등록하면 사이트가 다운됐을 때 이메일/슬랙 알림을 받습니다.

**3. GitHub Issues**
사이트 사용자에게 문제를 보고받을 수 있는 채널. 사이트 하단에 "문제 신고" 링크로 GitHub Issues 페이지를 연결하세요.

## 유지보수 체크리스트 (월 1회)

- [ ] 사이트 접속 확인 (모바일 포함)
- [ ] GitHub Dependabot 알림 확인 → \`npm audit fix\` 실행
- [ ] Vercel/Cloudflare 대시보드에서 빌드 에러 확인
- [ ] 환경변수 만료 여부 확인 (API 키에 따라 주기적 갱신 필요)
- [ ] Supabase 대시보드에서 사용량 확인 (무료 플랜 한도)

## 유지보수가 너무 부담스러우면

솔직히 유지보수를 하고 싶지 않은 분도 있을 것입니다. 그런 경우:

1. **정적 사이트로 유지**: DB나 API 없이 순수 HTML/CSS만 사용하면 거의 관리가 필요 없습니다
2. **원클릭 배포 템플릿 유지**: Linkmap 템플릿은 외부 의존성이 최소화되어 있어 유지보수 부담이 적습니다
3. **No-code 전환**: 유지보수를 완전히 맡기고 싶다면 [No-code vs Vibe Coding](/blog/nocode-vs-vibe-coding)을 참고하여 No-code 도구로 전환을 고려하세요

> **TIP:** [Linkmap 서비스맵](https://www.linkmap.biz)에서 프로젝트에 연결된 모든 서비스를 시각화하면, "어디서 문제가 생겼는지" 빠르게 파악할 수 있습니다.

---

*첫 배포가 아직이라면 [원클릭 배포 가이드](/blog/oneclick-deploy-linkmap-guide)를, 배포 전 점검이 필요하면 [출시 체크리스트](/blog/vibe-coding-launch-checklist)를 참고하세요.*`;
