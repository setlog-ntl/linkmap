export const content = `> **KEY:** 배포한 내 사이트가 "가끔" 안 열리는 이유, 알고 보면 Next.js가 보이지 않는 곳에서 요청을 52건이나 보내고 있었습니다. 이 글에서는 원인을 찾는 과정과 코드 한 줄로 해결한 방법을 초보자도 이해할 수 있게 설명합니다.

## 배포했는데 "가끔" 사이트가 안 열린다

[Linkmap](https://www.linkmap.biz)을 Cloudflare에 배포한 직후였습니다. 처음엔 잘 됐습니다. 그런데 며칠 뒤부터 이상한 리포트가 들어오기 시작했습니다.

"가끔 페이지가 안 열려요."

직접 접속해보면 잘 됩니다. 잠깐 기다렸다 다시 접속하면 또 됩니다. **코드를 전혀 바꾸지 않았는데**, 됐다 안 됐다를 반복합니다.

이런 류의 버그가 가장 골치 아픕니다. 항상 재현되면 원인을 찾기 쉬운데, "가끔"만 발생하니까요.

> **INFO:** 이 글에서 말하는 "503 에러"란? 웹사이트에 접속했을 때 "서비스를 일시적으로 사용할 수 없습니다"라는 뜻의 오류 코드입니다. 서버가 요청을 처리할 수 없을 만큼 바쁘거나, 리소스 한계를 넘었을 때 발생합니다.

## 먼저 시도한 것들 (효과는 있었지만 완전하지 않았다)

### 시도 1: 페이지를 미리 만들어두기

Next.js는 기본적으로 누군가 접속할 때마다 페이지를 새로 만듭니다. 이걸 "서버 사이드 렌더링(SSR)"이라고 합니다. 매번 만드는 대신, **빌드할 때 미리 만들어놓으면** 서버 부담이 줄어듭니다.

\`\`\`tsx
// Before — 매 접속마다 서버가 페이지를 새로 만듦
export default async function GuidePage() { ... }

// After — 빌드할 때 미리 만들어놓음 (서버 부담 없음)
export const revalidate = false;
export default async function GuidePage() { ... }
\`\`\`

효과가 있었습니다. 하지만 503은 여전히 가끔 발생했습니다.

### 시도 2: 데이터 파일 분리

블로그 포스트 데이터가 파일 하나에 3,574줄로 몰려 있었습니다. 이걸 작은 파일 여러 개로 쪼개서 필요할 때만 불러오도록 바꿨습니다.

### 시도 3: 불필요한 실시간 계산 제거

\`\`\`tsx
// Before — 접속할 때마다 현재 시간을 계산
lastModified: new Date()

// After — 미리 정해둔 날짜를 사용
const BUILD_DATE = '2026-03-19';
lastModified: BUILD_DATE
\`\`\`

이런 작은 최적화를 여러 개 적용했는데, 503 빈도는 줄었지만 **완전히 사라지지 않았습니다**. 뭔가를 놓치고 있었습니다.

---

## 범인을 찾다 — 보이지 않는 요청 52건

막막한 상황에서 [Playwright](https://playwright.dev)(브라우저를 자동으로 조작하는 테스트 도구)로 페이지를 방문하면서 브라우저가 보내는 요청을 전부 기록했습니다.

가이드 페이지(\`/guides/env\`)에 접속했을 뿐인데, 이런 오류들이 쏟아졌습니다:

\`\`\`
GET /showcase?_rsc=13zrp  503
GET /blog?_rsc=13zrp      503
GET /pricing?_rsc=13zrp   503
GET /feedback?_rsc=13zrp  503
GET /?_rsc=13zrp          503
GET /demo?_rsc=13zrp      503
\`\`\`

저는 \`/guides/env\` 하나만 방문했을 뿐입니다. 그런데 \`/pricing\`, \`/blog\`, \`/showcase\` 같은 다른 페이지들에 대한 요청이 **자동으로** 발생하고, 전부 503이 났습니다.

\`?_rsc=\` 라는 특이한 주소가 힌트였습니다.

> **WARNING:** \`?_rsc=\` 가 붙은 요청은 사용자가 보낸 것이 아닙니다. Next.js가 "혹시 사용자가 이 링크를 클릭할 수도 있으니 미리 데이터를 가져오자"라고 판단해서 자동으로 보내는 요청입니다. 이걸 **prefetch**(프리페치, 미리 가져오기)라고 합니다.

## prefetch가 뭔데 문제를 일으키나?

Next.js에서 페이지 이동에 쓰는 \`<Link>\` 컴포넌트는 **화면에 보이는 모든 링크의 목적지를 미리 가져오는** 기능이 기본으로 켜져 있습니다.

이게 일반적으로는 좋은 기능입니다. 사용자가 링크를 클릭하면 이미 데이터가 준비되어 있어서 페이지가 빠르게 전환되니까요.

문제는 **링크 개수**였습니다.

\`\`\`
사용자가 아무 페이지 방문
  → 상단 메뉴(Header) 렌더링 — 링크 31개
  → 하단 메뉴(Footer) 렌더링 — 링크 10개
  → 홈 페이지 본문 — 링크 11개
  → 총 52개 링크가 화면에 보임
  → Next.js가 52개 페이지 데이터를 동시에 미리 요청
  → Cloudflare 서버에 요청 폭탄
  → 503 에러 발생!
\`\`\`

상단 메뉴와 하단 메뉴는 **모든 페이지에 공통**으로 있습니다. 사용자가 어느 페이지를 방문하든, 매번 52건의 숨겨진 요청이 서버로 날아갔던 것입니다.

| 위치 | 링크 수 | 미리 보내는 요청 수 |
|------|--------|------------------|
| 상단 메뉴 (데스크톱) | 9개 | 9건 |
| 상단 메뉴 (로그인 메뉴) | 7개 | 7건 |
| 상단 메뉴 (모바일) | 15개 | 15건 |
| 하단 메뉴 | 12개 | 10건 |
| 홈 페이지 본문 | 11개 | 11건 |
| **합계** | **54개** | **52건** |

이전에 시도 4로 블로그/가이드 페이지 내부 링크에만 prefetch를 껐었는데, **상단 메뉴와 하단 메뉴는 전혀 손대지 않았습니다**. 그래서 503이 계속됐던 것입니다.

---

## 해결: 코드 한 줄 추가

해결은 놀랍도록 간단했습니다. 상단 메뉴, 하단 메뉴, 홈 페이지의 모든 링크에 "미리 가져오기 끄기"를 추가했습니다.

\`\`\`tsx
// Before — Next.js가 자동으로 이 페이지 데이터를 미리 가져옴
<Link prefetch={false} href="/pricing">가격</Link>
<Link prefetch={false} href="/blog">블로그</Link>

// After — 사용자가 클릭할 때만 데이터를 가져옴
<Link href="/pricing" prefetch={false}>가격</Link>
<Link href="/blog" prefetch={false}>블로그</Link>
\`\`\`

\`prefetch={false}\` — 이 한 줄이 전부입니다.

수정한 파일과 링크 수:

| 파일 | 수정한 링크 수 |
|------|-------------|
| 상단 메뉴 (\`header.tsx\`) | 31개 |
| 하단 메뉴 (\`footer.tsx\`) | 10개 |
| 홈 배너 (\`hero-section.tsx\`) | 3개 |
| CTA 버튼 (\`cta-section.tsx\`) | 2개 |
| 기타 홈 컴포넌트 | 6개 |

적용 후 Playwright 테스트를 다시 실행했습니다. **숨겨진 요청 52건 → 0건. 503 에러 완전 해결.**

> **TIP:** Cloudflare Workers에 Next.js를 배포한다면, 상단 메뉴와 하단 메뉴의 \`<Link>\`부터 \`prefetch={false}\`를 적용하세요. 이 컴포넌트들은 모든 페이지에 공통으로 존재하므로 링크가 많을수록 요청이 기하급수적으로 늘어납니다.

## "미리 가져오기를 끄면 사이트가 느려지지 않나?"

당연히 드는 의문입니다. prefetch는 사이트를 빠르게 만들려고 존재하는 기능인데, 꺼도 괜찮을까요?

**이 환경에서는 오히려 끄는 게 더 빠릅니다.**

prefetch가 켜져 있으면:
- 페이지 방문 시 52건의 요청이 서버에 동시 도달
- 서버 과부하 → 일부 요청 503 에러
- 사용자가 실제로 클릭한 페이지도 503이 될 수 있음

prefetch가 꺼져 있으면:
- 사용자가 실제로 클릭한 링크에 대해서만 1건의 요청 발생
- 미리 만들어둔(정적화된) 페이지는 전 세계 CDN 캐시에서 즉시 응답
- 503 없이 안정적으로 페이지 전환

Cloudflare는 전 세계 300개 이상의 서버에 정적 페이지를 캐시합니다. 미리 만들어둔 페이지라면 메인 서버까지 갈 필요 없이 **가장 가까운 서버에서 바로 응답**합니다. prefetch의 속도 이점보다 이쪽이 더 빠릅니다.

## 이 경험에서 배운 것

**1. Next.js 기본 설정은 Vercel 기준이다**

Next.js는 Vercel에 배포하는 것을 기본으로 설계되었습니다. Cloudflare Workers처럼 리소스 제한이 다른 환경에서는 기본 설정을 그대로 쓰면 문제가 생길 수 있습니다.

**2. 상단 메뉴·하단 메뉴가 성능의 핵심이다**

모든 페이지에 공통으로 들어가는 컴포넌트입니다. 여기서 발생하는 성능 문제는 사이트 전체에 영향을 미칩니다. 배포 후 가장 먼저 점검해야 할 곳입니다.

**3. "가끔 발생하는 버그"는 자동 테스트로 잡는다**

"가끔 503이 뜬다"는 리포트만으로는 원인을 찾기 어렵습니다. Playwright로 브라우저 요청을 자동 수집하니 정확히 어떤 요청이 실패하는지 바로 보였습니다.

**4. 눈에 보이지 않는 요청을 확인하자**

사용자가 클릭하지 않아도 브라우저가 보내는 요청이 있습니다. 개발자 도구 Network 탭에서 \`_rsc\`로 검색하면 prefetch 요청이 몇 건이나 발생하는지 확인할 수 있습니다.

## 내 사이트도 확인해보려면?

1. 브라우저에서 **F12** (개발자 도구) → **Network 탭** 열기
2. 내 사이트 접속
3. 필터에 \`_rsc\` 입력
4. 표시되는 요청 수 확인 — 이게 Next.js가 자동으로 보내는 숨겨진 요청입니다

요청이 10건 이상이라면 상단 메뉴와 하단 메뉴의 \`<Link>\`에 \`prefetch={false}\`를 추가하는 것을 추천합니다.

## 배포 후 점검 체크리스트

Cloudflare Workers + Next.js 조합을 사용할 때의 체크리스트입니다.

- [x] 공개 페이지 미리 만들어두기 (\`revalidate = false\`)
- [x] 빌드 때 정할 수 있는 값은 미리 정해두기 (\`new Date()\` 제거)
- [x] 상단 메뉴·하단 메뉴·공통 레이아웃의 Link → \`prefetch={false}\`
- [x] 가이드/블로그 내부 Link → \`prefetch={false}\`
- [ ] 번들 크기 정기 점검 (데이터 파일 분리, 필요할 때만 불러오기)
- [ ] 개발자 도구에서 \`?_rsc=\` 요청 수 정기 확인

> **TRY:** 비슷한 문제를 겪고 있다면 브라우저 개발자 도구 Network 탭에서 \`_rsc\`로 필터링해 보세요. prefetch 요청이 몇 건인지, 그 중 오류가 있는지 바로 확인할 수 있습니다. [Linkmap 배포 가이드](/guides/deploy)에서 Cloudflare Workers 배포 전반을 다룹니다.

---

*Cloudflare 연결 전반은 [Cloudflare 연결 가이드](/guides/cloudflare)에서, 이 시리즈의 첫 번째 이야기는 [ERP 담당자의 Linkmap 개발기 ① — 코드는 안 바꿨는데 서비스가 안 된다](/blog/linkmap-dev-story-1-infra-battle)에서 읽을 수 있습니다.*`;
