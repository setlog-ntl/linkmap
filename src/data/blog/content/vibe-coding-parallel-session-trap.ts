export const content = `
> **KEY:** "터미널 두 개 열면 두 배로 빠르겠지?" — 그렇게 생각했다. 한 시간 뒤, 오른쪽 창에서 만든 코드가 흔적도 없이 사라져 있었다.

## 그날, 작업이 사라졌다

바이브 코딩에 재미가 붙기 시작할 무렵이었다.

왼쪽 터미널에서는 AI에게 로그인 기능을 시키고, 오른쪽 터미널에서는 대시보드 UI를 고치고 있었다. 둘 다 같은 프로젝트 폴더를 열어놓은 채로. "서로 다른 파일 건드리니까 괜찮겠지" 하고 넘겼다.

한 시간쯤 지났을까.

왼쪽이 끝난 걸 확인하고 오른쪽으로 돌아갔더니 — 분명 고쳤던 부분이 원래대로 돌아가 있었다. 한 줄 두 줄이 아니라, 수정한 흔적 자체가 없었다.

처음엔 내가 저장을 안 한 줄 알았다. 다시 확인해봤다. 저장은 됐었다. 문제는 왼쪽 세션이 오른쪽에서 작업하던 파일을 **같이 건드렸다**는 것. 그리고 마지막에 저장한 쪽이 이겼다.

에러 메시지? 없었다. 경고? 없었다. 그냥 조용히 덮어써졌다.

> **WARNING:** 이 함정의 진짜 무서운 점은 **아무 경고도 없다**는 것이다. 에러가 났으면 바로 알아챘을 텐데, 멀쩡히 돌아가는 것처럼 보이다가 나중에 "어? 이 코드 내가 안 고쳤나?" 하고 뒤늦게 발견하게 된다.

## 원인은 단순했다

비유를 하나 들어보겠다.

**구글독스**에서 두 사람이 같은 문서를 편집하면, 서로의 커서가 보인다. 한 사람이 문장을 고치면 다른 사람 화면에도 실시간으로 반영된다.

그런데 **워드 파일**을 USB에 복사해서 각자 수정하면? 나중에 하나를 골라야 한다. 합칠 방법이 없다.

프로젝트 폴더는 워드 파일 쪽이다.

두 AI 세션이 같은 폴더에서 같은 파일을 편집하면, 나중에 저장한 쪽이 이긴다. Git을 쓰고 있어도 **커밋 전**이라면 Git은 관여하지 않는다. 커밋하기 전의 파일은 그냥 하드디스크 위의 일반 파일일 뿐이다.

> **INFO:** 이 문제는 [Claude Code](https://claude.ai/code), [Cursor](https://cursor.com), Windsurf 등 도구 종류에 관계없이 동일하게 발생한다. 같은 폴더를 공유하는 두 세션이라면 어떤 도구든 마찬가지다.

---

## 그래서 어떻게 해결했는가

세 가지 방법이 있다. 상황에 따라 골라 쓰면 된다.

### 1단계 — 영역을 나눠서 작업하기

가장 단순한 방법이다. "이 세션은 이 폴더만, 저 세션은 저 폴더만" 하고 규칙을 정한다.

- 세션 A: \`src/components/\` 안의 UI만 담당
- 세션 B: \`src/app/api/\` 안의 API만 담당

겹치는 파일이 없으면 덮어쓰기는 일어나지 않는다. 나도 처음엔 이렇게 했다. 블로그는 왼쪽 창, 서비스 설정은 오른쪽 창. 한동안은 문제가 없었다.

하지만 프로젝트가 커지면서 "완전히 다른 영역"이라고 생각했던 것들이 **공통 파일을 공유**하고 있다는 걸 알게 됐다. 타입 정의 파일, 설정 파일, 공통 유틸리티 — 이런 것들이 예상치 못하게 겹쳤다.

### 2단계 — Git 워크트리로 작업 공간을 물리적으로 분리하기

이 방법이 핵심이다.

**Git 워크트리**는 같은 저장소에서 여러 개의 독립된 작업 폴더를 만드는 기능이다. 비유하자면, 같은 집에서 각자 **방을 따로 쓰는 것**이다. 방이 다르니 서로의 물건을 건드릴 일이 없다.

\`\`\`bash
# 프로젝트 폴더 옆에 독립 작업 공간 생성
git worktree add ../my-project-login login-feature
git worktree add ../my-project-dashboard dashboard-fix
\`\`\`

이렇게 하면 \`my-project-login\` 폴더와 \`my-project-dashboard\` 폴더가 새로 생긴다. 같은 저장소를 바라보지만, 파일은 완전히 독립되어 있다.

왼쪽 터미널은 \`my-project-login\`에서 AI를 돌리고, 오른쪽은 \`my-project-dashboard\`에서 돌린다. 물리적으로 다른 폴더이기 때문에 **구조적으로 겹칠 수가 없다**.

> **TIP:** Claude Code를 쓴다면 각 워크트리 폴더에서 따로 \`claude\` 명령을 실행하면 된다. 세션마다 서로 다른 폴더를 기준으로 작동하므로, 같은 파일을 건드릴 일이 원천적으로 차단된다.

### 3단계 — 작업이 끝나면 합치기

워크트리에서 작업이 끝났다면, 결과물을 메인 브랜치로 합쳐야 한다.

\`\`\`bash
git checkout main
git merge login-feature
git merge dashboard-fix
\`\`\`

두 세션이 서로 다른 파일을 건드렸다면 자동으로 합쳐진다. 같은 파일의 같은 줄을 건드린 드문 경우에만 Git이 "어느 쪽을 쓸까요?" 하고 물어본다.

다 쓴 워크트리는 정리한다:

\`\`\`bash
git worktree remove ../my-project-login
\`\`\`

깨끗하게 원래 상태로 돌아온다.

---

## 돌아보며 — 언제 나누고 언제 그냥 할까

모든 작업에 워크트리가 필요한 건 아니다. 내가 경험으로 정리한 기준이다.

| 상황 | 방법 |
|------|------|
| 세션 하나로 순서대로 작업 | 그냥 사용 |
| 두 세션이 완전히 다른 폴더 담당 | 영역 분리 |
| 공통 파일을 건드릴 가능성이 조금이라도 있음 | 워크트리 |
| 여러 세션을 30분 이상 동시에 돌릴 예정 | 워크트리 강력 권장 |

가장 안전한 원칙은 이것이다: **병렬로 돌리는 순간, 폴더를 분리하라.**

나도 처음엔 "굳이 그렇게까지?" 했다. 그런데 한 시간짜리 작업이 사라지는 경험을 두 번 하고 나니 생각이 바뀌었다. 워크트리를 만드는 데 10초, 사라진 작업을 다시 하는 데 한 시간. 계산은 간단하다.

> **INFO:** Git 자체를 아직 안 쓰고 있다면 워크트리보다 Git부터 시작해야 한다. Git은 "AI가 코드를 망쳤을 때 되돌리는 유일한 방법"이기도 하다. [GitHub 시작하기 가이드](/guides/github)에서 설치부터 첫 커밋까지 따라할 수 있다.

## 빠르게 가려다 느려지는 함정을 피하는 법

바이브 코딩의 매력은 속도다. AI에게 시키면 몇 분 만에 기능이 나온다. 그래서 두 배로 빠르게 가고 싶어지는 건 자연스러운 욕심이다.

하지만 **같은 폴더를 공유하는 두 세션**은 두 배의 속도가 아니라, 한쪽이 사라지는 복불복이다. 워크트리는 그 복불복을 확실한 두 배로 바꿔준다.

10초의 준비로 한 시간의 좌절을 피할 수 있다면 — 그건 충분히 가치 있는 투자다.

> **TRY:** 여러 세션을 동시에 돌리면서 서비스 연결 상태까지 한눈에 관리하고 싶다면 [Linkmap 무료로 시작하기](https://www.linkmap.biz/signup) — 각 브랜치에서 연결 중인 외부 서비스와 환경변수를 시각화할 수 있다.

---

*Git 기초가 아직 낯설다면 [바이브코더를 위한 Git 기초 가이드](/blog/git-basics-for-vibe-coders)를, 버전 관리 개념 전체는 [버전 관리 가이드](/guides/version-control)를 참고하자.*
`;
