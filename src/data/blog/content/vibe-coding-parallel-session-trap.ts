export const content = `> **KEY:** 여러 AI 세션을 동시에 돌리면 빠르겠지 — 맞습니다. 그런데 같은 폴더를 두 세션이 함께 건드리면, 한쪽 작업이 다른 쪽 작업을 조용히 덮어씁니다. Git 워크트리로 작업 공간을 물리적으로 분리하면 이 함정을 완전히 피할 수 있습니다.

## "빠르겠지?" — 그리고 작업이 사라졌다

바이브 코딩에 익숙해지면 자연스럽게 드는 생각이 있습니다.

"터미널 두 개 열어서 동시에 돌리면 두 배로 빠르겠는걸?"

저도 그렇게 했습니다. 왼쪽 창에서는 Claude Code로 로그인 기능을 만들고, 오른쪽 창에서는 대시보드 UI를 고치고 있었습니다. 한 시간쯤 지났을까요. 왼쪽 세션이 끝난 걸 확인하고 오른쪽으로 넘어갔더니 — 오른쪽에서 수정했던 내용 일부가 없어져 있었습니다.

처음엔 내가 실수한 줄 알았습니다. 다시 확인해보니 왼쪽 세션이 오른쪽에서 작업하던 파일을 같이 건드렸고, 마지막에 저장된 쪽이 이긴 것이었습니다.

이것이 **병렬 세션 덮어쓰기 함정**입니다. 경험해본 사람은 압니다 — 굉장히 황당합니다.

> **WARNING:** 에러 메시지가 전혀 없다는 게 가장 위험합니다. 두 세션이 같은 파일을 동시에 편집하고 저장해도 아무 경고도 뜨지 않습니다. 나중에 "어? 이 코드 내가 짰나?" 하고 뒤늦게 알아채는 경우가 대부분입니다.

## 왜 이런 일이 생기는가 — 구글독스 vs 워드 파일 비유

이 현상을 이해하려면 간단한 비유가 도움이 됩니다.

**구글독스**에서 두 명이 같은 문서를 동시에 편집하면 어떻게 될까요? 서로의 커서가 보이고, 변경 사항이 실시간으로 합쳐집니다. 충돌이 생기면 알림이 뜹니다.

반면 **워드 파일**을 USB에 복사해서 두 명이 각자 수정하면? 두 파일을 합칠 방법이 없습니다. 나중에 보내온 파일이 이전 파일을 덮어씁니다.

프로젝트 폴더는 워드 파일에 가깝습니다. 두 AI 세션이 같은 폴더 안의 같은 파일을 편집하면, 나중에 저장한 쪽이 이깁니다. Git이 있어도 마찬가지입니다 — 커밋 전이라면 Git은 개입하지 않습니다. 작업 중인 파일은 그냥 하드디스크에 있는 일반 파일일 뿐이고, 마지막 쓰기 작업이 항상 우선합니다.

> **INFO:** 이 문제는 Claude Code, Cursor, Windsurf 등 AI 코딩 도구 종류에 관계없이 동일하게 발생합니다. 같은 폴더를 공유하는 두 세션이라면 어떤 도구든 마찬가지입니다.

## 해결 방법 3단계

좋은 소식은, 이 문제를 피하는 방법이 간단하다는 점입니다. 세 가지 수준의 해결책이 있고, 상황에 따라 골라 쓸 수 있습니다.

### 1단계 — 영역을 나눠서 작업 (가장 간단)

가장 쉬운 방법은 "세션 A는 이 폴더만, 세션 B는 저 폴더만" 하고 명확히 규칙을 정하는 것입니다.

예를 들어:
- 세션 A: \`src/components/\` 안의 UI 컴포넌트만 담당
- 세션 B: \`src/app/api/\` 안의 API 코드만 담당

두 세션이 겹치는 파일이 없다면 덮어쓰기는 일어나지 않습니다. 단점은 항상 이렇게 칼같이 나누기가 어렵다는 것입니다. 공통으로 쓰는 파일이 하나라도 있으면 위험해집니다.

**언제 쓰나**: 작업이 명확하게 분리되고 겹치는 파일이 없다고 확신할 때.

### 2단계 — Git 워크트리로 작업 공간 분리 (추천)

이 방법이 핵심입니다. **Git 워크트리(worktree)**는 같은 저장소에서 여러 개의 독립된 작업 폴더를 만들어주는 기능입니다.

비유하자면 이렇습니다. 같은 집에서 여러 사람이 각자 방을 쓰는 것처럼, 같은 저장소에서 세션마다 물리적으로 다른 폴더를 씁니다. 방이 다르니 충돌이 없습니다.

터미널에서 이렇게 입력합니다:

\`\`\`bash
# 워크트리 폴더 만들기 (프로젝트 폴더 밖에 생성)
git worktree add ../my-project-feature-a feature-a
git worktree add ../my-project-feature-b feature-b
\`\`\`

이제 \`my-project-feature-a\` 폴더와 \`my-project-feature-b\` 폴더가 생겼습니다. 두 폴더는 같은 저장소를 바라보지만, 각자 독립된 파일 공간을 가집니다. 세션 A는 \`feature-a\` 폴더에서, 세션 B는 \`feature-b\` 폴더에서 작업합니다 — 영원히 겹치지 않습니다.

**언제 쓰나**: 여러 기능을 동시에 개발할 때, 또는 어떤 파일이 겹칠지 확신하기 어려울 때. 사실상 병렬 세션을 쓰는 모든 상황에 권장합니다.

> **TIP:** Claude Code를 쓴다면 각 워크트리 폴더에서 따로 \`claude\` 명령을 실행하세요. 세션마다 다른 폴더를 기준으로 작동하므로, 같은 파일을 건드릴 일이 구조적으로 불가능해집니다.

### 3단계 — 작업 완료 후 합치기

워크트리 작업이 끝났다면, 두 브랜치를 합쳐야 합니다. 무서운 이야기처럼 들리지만 실제로는 간단합니다.

\`\`\`bash
# 메인 브랜치로 이동
git checkout main

# feature-a 브랜치의 내용을 가져와서 합치기
git merge feature-a

# 그다음 feature-b도 합치기
git merge feature-b
\`\`\`

두 브랜치가 서로 다른 파일을 건드렸다면 합치는 과정이 자동으로 완료됩니다. 같은 파일의 같은 줄을 건드렸다면 Git이 "이 부분은 어떻게 할게요?" 하고 물어봅니다. 그때 직접 골라주면 됩니다. 이것이 머지 컨플릭트인데, 1단계처럼 영역을 잘 나눴다면 거의 발생하지 않습니다.

다 쓴 워크트리는 정리합니다:

\`\`\`bash
git worktree remove ../my-project-feature-a
\`\`\`

---

## 언제 분리하고 언제 그냥 작업해도 될까

항상 워크트리가 필요한 건 아닙니다. 판단 기준을 정리해봤습니다.

| 상황 | 권장 방법 |
|------|----------|
| 세션 하나로 순서대로 작업 | 그냥 사용, 문제 없음 |
| 두 세션이 완전히 다른 폴더 담당 | 1단계 (영역 분리) |
| 두 세션이 공통 파일을 건드릴 가능성 있음 | 2단계 (워크트리) |
| 여러 세션을 오래 동시에 돌릴 예정 | 2단계 (워크트리) 강력 권장 |
| 빠른 실험 후 버릴 코드 | 그냥 사용 가능 |

가장 안전한 원칙은 이것입니다: **병렬로 돌리면서 같은 폴더를 공유하는 순간, 워크트리를 쓰세요.**

> **INFO:** [Linkmap](https://www.linkmap.biz)은 현재 연결된 서비스와 환경변수를 한 화면에서 시각화합니다. 워크트리로 여러 브랜치를 동시에 개발할 때, 각 브랜치에서 사용하는 서비스 연결이 어떻게 다른지 파악하는 데 도움이 됩니다.

## 바이브코딩에서 Git을 안 쓰고 있다면

Git 자체를 아직 안 쓰고 있다면, 워크트리 이전에 Git부터 시작하는 게 먼저입니다. Git은 "AI가 코드를 망쳤을 때 되돌리는 유일한 방법"이기도 하고, 병렬 세션 문제를 포함한 여러 위기 상황의 구명보트입니다.

[GitHub 시작하기 가이드](/guides/github)에서 Git 설치부터 첫 커밋까지 한 번에 따라할 수 있습니다. Git을 모른다면 워크트리보다 이쪽을 먼저 읽기를 권합니다.

---

## 오늘 바로 할 수 있는 체크리스트

- [x] 지금 하나의 세션만 돌리고 있다면 이 문제는 해당 없음
- [ ] 두 개 이상의 세션을 동시에 쓸 계획이라면, 어떤 파일이 겹치는지 먼저 확인
- [ ] 겹치는 파일이 있거나 불확실하다면 워크트리 생성 후 각 세션을 분리된 폴더에서 실행
- [ ] 작업 완료 후 두 브랜치를 main에 합치고 워크트리 삭제
- [ ] [Linkmap 서비스 맵](https://www.linkmap.biz/services)으로 각 브랜치에서 연결 중인 외부 서비스 파악

> **TRY:** 병렬 세션을 안전하게 쓰고 싶다면 [Linkmap 무료로 시작하기](https://www.linkmap.biz/signup) — 서비스 연결 현황을 시각화하고, 각 브랜치·환경별로 어떤 API 키와 외부 서비스가 연결됐는지 한눈에 관리할 수 있습니다.

---

*Git 기초가 아직 낯설다면 [바이브코더를 위한 Git 기초 가이드](/blog/git-basics-for-vibe-coders)를, 버전 관리 개념 전체는 [버전 관리 가이드](/guides/version-control)를 참고하세요.*
`;
