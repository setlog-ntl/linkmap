# 2026-06 작업 로그

### 2026-06-29 — 비밀키 허브 단순화: 환경 구분 숨김 + 빠른설정·서비스페이지 상태점검 제거
- **카테고리:** #UX #기능 #정리
- **중요도:** P1
- **지시:** ① 비밀키 관리에서 보안메모도 함께 보이게(이미 완료 확인), ② 환경변수의 개발/스테이징/프로덕션 구분이 헷갈리니 제거(등록·관리 시 기본값 자동 적용, 필요할 때만 수정), ③ 환경변수+보안메모를 한 화면에서 보고 작성·수정(이미 완료 확인), ④ "빠른 설정" 기능 제거, ⑤ "상태 점검"이 실제로 점검하는지 확인 후 안 되면 삭제.
- **맥락:** 6/26 통합 허브로 env·credential·note는 이미 한 화면(타입탭+서비스그룹)에 병합됨 → ①③은 충족 상태. 남은 혼란 요인은 (a) 허브의 환경 필터·행 배지·빠른추가 환경 선택기로 노출되는 dev/staging/prod 구분, (b) 서비스 페이지 아코디언의 "빠른 설정"(SetupWizard)·"상태 점검" 버튼, 카탈로그/목록/맵의 "빠른 설정" 링크. 상태점검 실태 조사 결과: **8개 주요 서비스(stripe·supabase·anthropic·openai·clerk·resend·vercel·sentry)는 실제 외부 API 호출로 키 유효성(401) 검증 = 진짜 작동**, 나머지는 generic 어댑터가 환경변수 "존재 여부"만 확인. health-score는 연결(0.4)+health_check(0.3)+env완성도(0.3) 3신호 가중합이고 대시보드/서비스맵/`/monitoring`에 깊게 통합(49파일) → 전면 제거는 핵심 위젯 파괴. **사용자 확정: 상태점검은 "서비스 페이지 관련만" 제거(대시보드·서비스맵·`/monitoring` 유지), 빠른설정은 마법사+카탈로그/맵 링크까지 제거, 환경은 기본 자동 적용·편집 시에만 수정.**
- **결과:**
  - **환경 구분 숨김(허브 UI 한정, DB·암호화 무변경)**: 허브 환경 필터 Select·`envFilter` 상태·필터 로직 제거(`credentials/page.tsx`), 행 환경 배지 제거(`vault-item-row.tsx`), 빠른추가 환경 선택기 제거→기본값(env=development, cred/note=all) 자동 적용(`quick-add-dialog.tsx`), `vault-shared.ts`에서 `envFilterOptions`·`envBadgeLabel` 제거. **편집(`vault-edit-dialog.tsx`)에는 환경 선택기 유지** = "필요할 때만 수정" 경로. DB 컬럼·마이그레이션 손대지 않음(되돌리기 쉬움, 보안 위험 0).
  - **빠른설정 제거**: 서비스 페이지 아코디언의 "빠른 설정"(SetupWizard) 버튼·렌더·상태 제거(`services-content.tsx`), 카탈로그 2곳·서비스 목록 아이콘·서비스맵 디테일시트의 "빠른 설정" 링크 제거. 고아 컴포넌트 삭제: `setup-wizard.tsx`·`setup-architecture-preview.tsx`·`setup-live-logs.tsx`. 잔재 prop(`guideServiceIds`/`hasGuide`) 체인 정리(`service-catalog-client.tsx`·`service-list-item.tsx`·`services/page.tsx`) — `guidesData` fetch는 "키 설정"(guideApiKeyMap)용으로 유지.
  - **상태점검(서비스 페이지 관련만) 제거**: 서비스 페이지 "상태 점검" 버튼·`useRunHealthCheck` 사용 제거(`services-content.tsx`), 프로젝트 오버뷰 인라인 health 패널(`HealthContent`)+"상세 모니터링 보기" 링크 제거(`project/[id]/page.tsx`). 고아 삭제: `health-content.tsx`·`health-sparkline.tsx`·`service-map/views/health-score-ring.tsx`·`lib/utils/health-score.ts`(+테스트). **유지(사용자 선택)**: `/monitoring`(MonitoringDashboard, 대시보드 위젯이 링크)·health-check API·어댑터·`useHealthChecks`/`useLatestHealthChecks`/`useRunHealthCheck`·대시보드 health 위젯·서비스맵·`/env` 검증·`health-timeline`·`health-summary-card`.
  - 검증: `typecheck` 0 errors · 편집 파일 `eslint` 신규 경고 0(잔여 1건은 기존 reset-effect 패턴) · 삭제 모듈 dangling 참조 0.
- **미적용(후속 제안)**: `/env` "고급 도구" 페이지는 여전히 dev/staging/prod 탭 보유(파워유저용 advanced 표면이라 이번 범위 제외). 대시보드/`/monitoring`의 health-check 실행 버튼도 유지(사용자가 대시보드 유지 선택). 둘 다 원하면 별도 작업으로 제거 가능.
- **인사이트:** "전부 제거"의 표면적 요청 뒤 실제 의존 그래프(health-score 3신호·49파일·대시보드 링크)를 먼저 매핑하니, 안전한 절단면은 *실행/표시 분리*였음 — RUN 경로(API·뮤테이션)는 살리고 **서비스 페이지+오버뷰의 프롬프트성 UI만** 걷어내면 대시보드 read-only 위젯이 그대로 동작. 환경 구분도 테이블을 건드리지 않고 *표시·입력 레이어*에서만 숨기니 보안/데이터 무변경으로 "기본은 손 안 가게, 필요할 때만 수정" UX 성립.

### 2026-06-26 — 비밀키 관리 통합 허브 신설 (민감값 3종 통합 + 입력·조회 단순화)
- **카테고리:** #기획 #기능 #UX
- **중요도:** P1
- **지시:** 보안메모 데이터를 비밀키 관리에서 쉽게 보게 하고, 비밀키·환경키 입력·조회 과정을 최대한 심플하게 — 현재 등록·확인이 번거롭고 단계가 복잡하니 고도화 기획·적용.
- **맥락:** 민감값이 3개로 파편화 — 환경변수(`/env`)·보안메모(`/secure-notes`)는 탭 있고, 비밀키(`/credentials`)는 완성돼 있으나 **탭 없는 고아 페이지**(page-context "인증정보"로만 존재). 세 화면이 각자 복호화·MFA·마스킹·다이얼로그를 따로 가져 "어디에 뭘 넣었는지" 흩어져 보임. 또 인라인 MFA UI(`useMfaChallenge`/`MfaChallengeDialog`)가 구현돼 있으나 **어디서도 연결되지 않아** MFA 등록자가 👁 클릭 시 인증 모달 없이 에러 토스트만 뜨는 막다른 길. 사용자 확정: ①셋 다 단일 허브(타입탭+서비스별 그룹) ②통합 빠른추가(타입선택→핵심필드+상세접기) ③세션 내 MFA 캐싱.
- **결과:**
  - **DB·API 무변경**: 3개 테이블·3개 API·`encrypt/decrypt`·`requireMfa`·`logAudit` 그대로. 통합은 **UI 레이어 표시모델 병합**으로만 수행(보안규칙 준수).
  - **표시모델·매퍼**: `src/types/vault.ts`(`VaultItem`/`VaultKind`/`RevealedValue`)+barrel, `src/lib/mappers/vault.ts`(`toVaultItems`·`groupByService`), `src/components/credentials/vault-shared.ts`(옵션·`getPasswordStrength`·`kindMeta`).
  - **허브**(`/project/[id]/credentials` 재구성): 3종 fetch→병합, 타입탭(전체/환경변수/비밀키/보안메모, `?type=` 동기화는 `history.replaceState`로 Suspense 회피), 통합 검색·환경필터, 서비스별 `VaultGroup`(Collapsible+kind카운트)+`VaultItemRow`(마스킹·reveal·copy·edit·delete). 기존 고아 페이지 내용은 `/credentials/advanced`로 이전(일괄수정·.env내보내기 보존, "고급 도구" 링크).
  - **통합 빠른추가**(`quick-add-dialog.tsx`): 타입 라디오→핵심필드+`상세 설정`접기. env는 `KEY=VALUE` 붙여넣기 자동분리(`parseEnvLine`)+`NEXT_PUBLIC_` 자동 공개판정. 기존 add 뮤테이션·Zod 그대로.
  - **통합 편집**(`vault-edit-dialog.tsx`): VaultItem 받아 타입별 전체폼 복호화 사전채움→기존 update 뮤테이션.
  - **인라인 MFA**: 모든 복호화를 `executeSensitive`로 래핑→403 MFA_REQUIRED 시 인라인 OTP 모달→검증 후 세션 aal2 유지로 추가 인증 없이 연속 조회(=세션 캐싱), 감사로그는 매 복호화 기록. 취소 시 스피너 정리.
  - **네비·리다이렉트**: `project-tabs` 탭 3→1("비밀키 관리", `/env`·`/secure-notes` 제거), `/secure-notes`→`/credentials?type=note` 서버 리다이렉트, `page-context` 라벨 갱신. `/env`는 고급도구로 유지(링크).
  - 검증: `typecheck` 0 · `eslint` 0 errors(신규파일 경고 0). 인증 필요 페이지라 미인증 라우트는 307(미들웨어)로, 풀 브라우저 E2E는 로그인 세션 필요 — 미수행.
- **인사이트:** 복잡함의 근원은 기능이 아니라 *표면 파편화*였음 — 테이블/API를 합치지 않고 UI 레이어에서 `VaultItem`으로 병합하니 보안 규칙(암호화/RLS/감사 무변경)을 지키면서 "한 서비스 아래 3종" 뷰가 성립. "세션 내 캐싱"은 커스텀 타이머가 아니라 *이미 만들어졌지만 연결 안 된* `useMfaChallenge`를 잇는 것만으로 달성(Supabase aal2가 세션 유지) — 보안 약화 0. 고아 고급페이지는 삭제 대신 `/advanced` 이전+링크로 회귀 위험 없이 단순화.

### 2026-06-25 — 서비스 수동 등록 고도화 + 보안 메모 2-Type 시스템 신설 (Phase 1)
- **카테고리:** #기획 #기능 #DB
- **중요도:** P1
- **지시:** ① 서비스 목록에서 변수를 쉽게 설정·저장하도록 "빠른 설정" 앞에 "수동 등록" 버튼 추가. ② 환경변수(KEY=VALUE) 외에 백업코드·비밀번호 같은 자유 텍스트를 비개발자도 쉽게 저장하도록, 텍스트 저장(심플) 방식과 환경변수 형식 방식을 2가지로 구분해 관리하는 전체 기능 고도화 + 문서화.
- **맥락:** 기존 민감값 저장이 `environment_variables`(KEY=VALUE 설정값) 중심이라, 키 형식 강제·"키1=값1줄" 구조 때문에 백업코드 묶음·복구 시드 문구 같은 자유 텍스트를 담을 자리가 없었음. 기존 `service_credentials`는 ID/PW 로그인 계정 전용 구조라 부적합 → 자유 텍스트 전용 신규 타입이 올바른 아키텍처. 1순위 타겟 바이브코더(코딩 경험 0)도 개발자 문법 없이 저장 가능해야 함.
- **결과:**
  - **기획·문서**: `docs/planning/2026-06-25_secure-notes-2type.md` 신설(배경·2-Type 설계·데이터모델·보안·UI·구현단계·향후확장). `docs/db-schema.md`에 `secure_notes` 테이블+매핑 추가.
  - **수동 등록 진입점**(직전 작업 확장): 각 서비스 "수동 등록" 버튼 → `ManualRegisterDialog`(2탭: 환경변수/보안 메모). 환경변수 탭은 행 기반 입력+`.env 붙여넣기`(`ManualEnvForm`), 보안 메모 탭은 제목·분류·본문·환경·설명(`SecureNoteForm`). 둘 다 `service_id` 자동 연결.
  - **DB**(M103 `103_secure_notes.sql`): `secure_notes`(title·category 8종·encrypted_content·environment·notes) + 인덱스2 + RLS(소유자 FOR ALL + 팀 SELECT, `service_credentials` 패턴 미러). 암호화는 기존 AES-256-GCM(`src/lib/crypto`) 재사용.
  - **API**(5단계 패턴): `/api/secure-notes`(GET/POST/PATCH/DELETE) + `/api/secure-notes/decrypt`(MFA 가드 `requireMfa` — 미등록자 통과 / 등록자 aal2 필요, 감사로그). 입력 전부 Zod `safeParse`. 감사 action `secure_note.{create,update,delete,decrypt}` 4종 추가.
  - **타입·쿼리**: `src/types/secure-note.ts`(`SecureNote`/`SecureNoteCategory`)+barrel+`DbSecureNote*`, `src/lib/validations/secure-note.ts`, `src/lib/queries/secure-notes.ts`(use{SecureNotes,Add,Update,Delete,Decrypt}), `keys.ts`/`stale-time.ts`/`constants/secure-note.ts`.
  - **관리 화면**: `/project/[id]/secure-notes` 신설(목록·분류 필터·검색·추가/수정/삭제·복호화 표시/복사, 마스킹 기본). 프로젝트 탭에 "보안 메모"(ShieldCheck) 추가 — i18n 동결로 `fallbackLabel` 사용.
  - 검증: `typecheck` 0 · 변경 11파일 `eslint` 0. 마이그레이션 103은 Supabase 적용 필요(미적용 시 목록 빈 상태로 안전 동작).
- **인사이트:** "환경변수로 다 담으려는" 압박을 값의 *성격*(설정값 vs 자유 텍스트)으로 갈라 별도 테이블·별도 관리화면으로 분리하니, 비개발자 UX(제목+본문 메모)와 개발자 UX(KEY=VALUE)가 한 진입점(탭)에서 자연스럽게 공존. 기존 env/credential의 암호화·복호화·MFA·RLS·쿼리 패턴을 그대로 미러링해 신규 보안 표면을 최소 위험으로 추가 — 새 암호화/인증 코드를 만들지 않는 게 핵심.

### 2026-06-18 — 원클릭 배포 하네스 통합 + 모바일 초대장 고도화 (Phase-1)
- **카테고리:** #기획 #기능 #디자인
- **중요도:** P1
- **지시:** `linkmap/linkmap/harness-system/` 자료를 분석해 새로 정리한 서브에이전트·스킬을 검토·검증·적용하고, 현재 원클릭 초대장을 해당 자료 기준으로 업데이트하며, 하네스 시스템을 구축할 수 있게 기획·고도화.
- **맥락:** 사용자가 작성한 하네스 기획 패키지(5단계 파이프라인·단일 `site_blueprint`·신규 스킬3/서브에이전트3·글래스모피즘 초대장 디자인스펙)가 실제 코드와 두 가지 근본 충돌 — ① 하네스는 *자체 호스팅*(site_blueprint·`/s/[slug]`·인라인 편집·RSVP/분석) 전제인데 현재 원클릭은 *GitHub Pages 정적 내보내기*라 RSVP수집·분석·재발행 불가, ② 하네스 디자인은 글래스모피즘인데 CLAUDE.md는 앱 UI 글래스 금지. 사용자 결정으로 **① 단계적(phased): 지금은 내보내기 모델 위 고도화+에이전트 정비, 호스팅형은 설계만, ② 배포물 한정 글래스(6번째 minimal-glass 프리셋, 앱 UI 규칙·기존 5프리셋 유지)**.
- **결과:**
  - **Track A(.claude 정비)**: 하네스 스킬3·서브에이전트3을 `.claude/skills/`·`.claude/agents/`에 배치. **활성 2종** — `invitation-template-designer`(호스팅 sections/tokens 가정을 실제 스키마·제너레이터·프리셋·프리뷰 파일로 적응), `design-qa-reviewer`(읽기전용 QA 게이트, 7항목). **Phase-2 보류 4종** — `oneclick-deploy-architect`·`module-editor-builder`·`template-generator`·`deploy-integrator`: 트리거를 "호스팅형/site_blueprint/`/s/[slug]`" 전용으로 좁혀 활성 `원클릭` 오케스트레이터와 비충돌, 상단 "STATUS: Phase 2(미구현)" 배너+회송 지시. 기존 `oneclick-orchestrator.md`(invitation 11모듈·5단계↔export 매핑 표)·`tpl-invitation.md`(7→11모듈, minimal-glass, 신규 필드 스펙, 3중등록·키동기화·글래스 제약) 업데이트.
  - **Track B(초대장 고도화, export 모델)**: 신규 옵션 모듈 4종 — `message`(인사말)·`share`(카카오/복사/QR)·`rsvp`(외부 폼 링크)·`footer`(마무리+Powered by 토글, 기존 하드코딩 푸터를 토글 모듈로 전환). 신규 프리셋 `minimal-glass`(#a78bfa→#f9a8d4, `--inv-glass-*` 토큰 + `@supports not (backdrop-filter)` 솔리드 폴백을 제너레이터 주입 CSS·프리뷰 CSS 양쪽). share QR=외부 이미지 URL·카카오=SDK/Web Share 폴백·URL은 useEffect `window.location` 계산(SSG baking 회피), **신규 npm 의존성 0**. 변경 6파일(schema·generator·presets·template-def·preview·integrity-test에 invitation 검증대상 추가). tpl-invitation 서브에이전트 위임으로 구현(오케스트레이션 실증) 후 본체가 잔여 결함 1건(template string 내 stray interpolation `${config...}`) 직접 수정.
  - **Track D(통합 기획서)**: `docs/planning/2026-06-18_oneclick-harness-integration.md` 신설 — 결정기록·Track A/B 결과·5단계↔export 매핑·**Phase-2 호스팅형 설계(site_blueprint 스케치·`/s/[slug]`·발행/편집/분석·RLS, 단 기존 my-sites/publish 선검증·재사용 우선)**·90_의사전달 결정값·50_체크리스트(Phase-1 완료/Phase-2 보류 분리).
  - 검증: `typecheck` 0 · `eslint` 0 · `template-integrity`+`template-deps` 76 tests pass.
- **인사이트:** 사용자 기획 패키지가 "이상적 호스팅 모델"을 전제할 때, 현행 아키텍처(정적 내보내기)와의 간극을 *충돌로 표면화해 사용자 결정으로 가른 뒤* 단계적으로 흡수하는 게 안전 — 활성/Phase-2 분리 + 트리거 문구 분리로 미구현 자산이 활성 경로를 오염시키지 않게 함. 글래스모피즘 같은 전역 규칙 충돌은 "앱 chrome vs 배포 산출물" 경계로 해소(배포물은 사용자 콘텐츠라 규칙 적용 대상 아님). 서브에이전트 위임 산출물은 template literal 내 stray interpolation 같은 미세 결함이 남을 수 있어 typecheck가 최종 안전망 — 위임 후 본체 검증은 필수.

### 2026-06-16 — 용어사전 모바일 대응 + 향후 유지용 문서화
- **카테고리:** #디자인 #콘텐츠
- **중요도:** P2
- **지시:** 용어사전을 모바일에서 잘 보이게 확인·검토·업데이트하고, 이후 업데이트도 모바일에 적용되도록 문서화·정리.
- **맥락:** 1순위 타겟 바이브코더 상당수가 모바일 진입. 신설 용어사전을 360~375px 뷰포트로 Playwright 점검해 실제 문제를 도출하고, 데스크톱·모바일 동일 컴포넌트의 반응형 규칙을 못박아 향후 용어 추가·컴포넌트 수정 시에도 깨지지 않게 문서화.
- **결과:**
  - **진단(Playwright 360/375px)**: 가로 오버플로우 0(목록·상세, 모노 예시 URL 포함)·카드 1열 정상 확인. 실제 결함 3건 도출 — ① 히어로 부제 `<br hidden sm:block>` 모바일 숨김 시 단어 붙음("있도록비유") ② 필터·관련어 칩 34px(<44px 터치 가이드) ③ 검색 X버튼 아이콘 크기 탭영역.
  - **수정**: ① 부제 `<br>` 앞 `{' '}`로 공백 보존 ② 필터·관련어 칩 `min-h-[44px] sm:min-h-0`(모바일 44px/데스크톱 컴팩트) ③ 검색 X `h-11 w-11`+입력 `pr-14` ④ 예시 mono `break-words`(긴 토큰 방어). 재검증: 칩 44px·오버플로우 0·공백 복구 실측 통과.
  - **문서화**: `docs/glossary-system.md` 신설(구성 파일·용어 추가법·**모바일 규칙 표+회귀 체크리스트**·Workers 주의). 두 컴포넌트 상단에 모바일 규칙 요약 주석. `CLAUDE.md` Reference Docs 표에 등재. [[mobile-foundation]] 컨벤션 준수.
  - 검증: typecheck/lint 에러 0.
- **인사이트:** 모바일 회귀는 "눈"보다 `browser_evaluate`로 수치 측정(scrollWidth 오버플로우·터치타깃 height)이 빠르고 확실 — 칩 34px, 히어로 단어 붙음은 스크린샷만으론 놓치기 쉬움. 반응형 줄바꿈 `<br hidden sm:block>`는 모바일에서 인접 텍스트가 공백 없이 붙는 함정이 있어 `{' '}` 보존이 필수. 데이터 주도 페이지는 컴포넌트가 모바일을 책임지므로, 규칙을 컴포넌트 주석+문서에 못박아 두면 향후 데이터(용어) 추가가 자동으로 모바일 안전해짐.

### 2026-06-16 — 용어사전 시스템 신설 + 블로그·가이드 용어 78개 비유 고도화 (Phase 1+2 완료)
- **카테고리:** #콘텐츠 #기능 #디자인
- **중요도:** P1
- **지시:** 가이드 옆에 "용어사전" 버튼+검색을 추가하고, 블로그·가이드의 용어를 정리해 초보자가 비유·디자인·설명으로 쉽게 찾는 사전형 페이지를 구축. 적용 전 2개 용어로 전체 흐름을 먼저 만들고, 이후 정리한 단어들을 고도화 기획으로 업데이트하는 형태로 운영.
- **맥락:** 2층 퍼널의 "성장" 단계에서 바이브코더(코딩 경험 0)가 가이드·블로그를 읽다 모르는 용어에 막히는 마찰이 있었음. 기존 `/glossary`는 한/영 정의만 나열한 정적 페이지로 검색·비유·상세가 없어 "찾아보는 사전"으로 기능하지 못했음. 전면 재구축에 앞서 전체 흐름을 2개 샘플로 먼저 검증(샘플→고도화 2단계 운영).
- **결과:**
  - **Phase 1(전체 흐름 샘플) 완료**: 기존 `/glossary`(정적 나열형)를 **검색 가능한 사전형 시스템으로 업그레이드**(중복 페이지 미생성, SEO 자산 유지).
  - **데이터 모델 신설** (`src/data/seo/glossary-terms.ts`): `GlossaryEntry`(slug·난이도·이모지·oneLiner·**analogy(비유)**·definition·example·aliases(검색)·related(관련어)·sources(가이드 연결)). 기존 33개 용어 마이그레이션 + **2개 완전 고도화 샘플(환경변수·API 키)** — 비유·실제예시·관련어·가이드 3종 연결 포함.
  - **검색·필터 UI** (`glossary-browser.tsx`, client): 즉시검색(term/termEn/oneLiner/alias/slug 매칭 — "env"→환경변수 별칭 매칭 실증), 카테고리 칩(7종+개수), 비유 포함 뱃지·난이도 뱃지, 무결과 폴백.
  - **상세 페이지** (`/glossary/[slug]`, SSG `generateStaticParams`+`revalidate=false`, blog/services와 동일한 Workers-safe 패턴): 비유 콜아웃(쉽게 말하면)·자세히·영문정의·실제예시(mono)·관련용어 칩·더 알아보기(가이드 연결)·돌아가기. 용어별 `generateMetadata`(canonical/OG)+**DefinedTerm JSON-LD**.
  - **헤더 진입점**: 가이드 옆 "용어사전" 버튼(데스크탑 nav + 모바일 시트), `Library` 아이콘.
  - **SEO 배선**: `sitemap.ts`에 33개 용어 URL 등록, `json-ld.ts`에 `generateGlossaryTermJsonLd`(DefinedTerm+Breadcrumb), 리스트는 DefinedTermSet 유지.
  - 검증: typecheck/lint(에러 0) 통과. Playwright로 리스트·검색(별칭)·상세(비유 콜아웃) 전 흐름 실증.
  - **Phase 2(전체 고도화) 완료**: ① 블로그(56개 본문)·가이드(40+개) 콘텐츠를 3개 병렬 에이전트로 스캔해 미수록 용어 추출(블로그/개념가이드/배포가이드 분담) ② 추출·중복제거한 **신규 45개**를 비유·예시·관련어·가이드 연결까지 전부 완성해 추가 → **총 78개**. ③ 기존 31개도 비유·예시·가이드 연결 일괄 고도화 → **78개 전부 비유 보유(gold-standard 100%)**. ④ 보안 용어 다수 유입에 맞춰 `security`(보안·취약점) 카테고리 신설, 기존 `auth`는 '인증·권한'으로 정리(CORS·AES는 security로 이동). ⑤ 데이터 무결성 자동검증(tsx): 중복 slug 0, 관련어 slug 100% 해소, 가이드 href 100% 실재 확인. 카테고리 분포 — backend 18·devops 16·security 12·ai 8·frontend 8·core 6·infra 6·auth 4.
  - 신규 용어 예: AI 에이전트·MCP·컨텍스트 윈도우·환각 / XSS·CSRF·SQL 인젝션·프롬프트 인젝션·HTTPS·SSL/TLS·동일출처정책·입력검증·하드코딩·시크릿스캐닝 / HTTP 상태코드·Bearer 토큰·스키마·RDB·NoSQL·CRUD·외래키·마이그레이션·미들웨어·GraphQL·멱등성·WebSocket·PG사·빌링키·가상계좌 / 하이드레이션·SEO·반응형 / A레코드·CNAME·localhost / 브랜치·PR·머지충돌·의존성·시맨틱버전·.gitignore·GitHub Actions·YAML·Preview배포·피처플래그.
- **인사이트:** "정의를 나열하는 용어집"과 "찾아보는 사전"은 다른 제품 — 검색(별칭 포함)·비유·상세 라우트가 있어야 초보자가 가이드 읽다 즉시 참조한다. enriched 모델에 optional 필드(analogy/example/related/sources)를 두면 기존을 깨지 않고 2개만 먼저 gold-standard로 만든 뒤 나머지를 점진 고도화할 수 있어 "샘플→확장" 운영과 데이터 구조가 정확히 일치. 용어 추출은 콘텐츠 영역을 3개 에이전트로 병렬 분담(블로그/개념/배포)하고 비유·예시 작성과 slug·href 정합성은 오케스트레이터가 직접 책임지는 분업이 효율적 — 에이전트는 "무엇을 넣을지"(추출), 본체는 "어떻게 쓸지+링크 무결성"을 맡음. 추출 과정에서 보안 용어 비중이 드러나 카테고리 체계(security 신설)까지 데이터가 역으로 설계를 개선.

### 2026-06-15 — 모바일 UX/UI 전면 고도화 (PC↔모바일 어색함 일소)
- **카테고리:** #디자인 #리팩터
- **중요도:** P1
- **지시:** PC에선 자연스럽지만 모바일에서 어색하게 보이는 전체 기능·화면을 UX/UI 전문가 관점에서 진단하고, 어색한 부분을 모두 보완해 자연스럽게 고도화·업데이트.
- **맥락:** 1순위 타겟인 바이브코더는 상당수가 모바일로 진입하는데, 데스크톱 우선 설계의 잔재(고정 px 폭, 데스크톱 오버레이, hover 전용 인터랙션, <44px 터치 타겟)가 전 화면에 산재. 5개 영역 병렬 정밀 진단으로 ~60건을 도출하고 근본 원인 4패턴으로 묶어 일괄 보완.
- **결과:**
  - **Phase 0 전역 토대(전 화면 영향)**: `layout.tsx`에 `viewport`(viewport-fit=cover, 사용자 확대 허용) export 추가. `globals.css`에 모바일 토대 유틸 신설 — `pb-safe`/`pb-safe-bar`/`bottom-safe-6`/`touch-target`(44px), 가로 오버플로우 가드(`body { overflow-x: clip }`, sticky 비파괴), 탭 하이라이트 제거, iOS 글자 자동확대 방지, `--header-height`(3.5rem) 토큰. 전역 `Dialog` 반응형 패딩(`p-4 sm:p-6`)+`max-h-[90dvh] overflow-y-auto`, `Sonner` 토스트 모바일 상단중앙(하단 FAB 충돌 회피).
  - **① 랜딩(13건)**: Hero React Flow 모바일 고정줌(0.6)+반응형 노드 배치(`NODE_OFFSETS_MOBILE`)로 인터랙션 복구, Hero 높이 압축 해소, Footer 1열, 블로그 복사버튼 터치 노출, 쇼케이스 필터 터치영역, 가격 태블릿 2열. **버그 수정**: `how-it-works`의 동적 Tailwind 클래스(`border-${activeColor}`)가 컴파일 안 되던 것 정적화.
  - **② 서비스맵·env(14건)**: 서비스 상세 패널 → 모바일 바텀시트(`Sheet side=bottom`) 전환(380px 오버플로우 해소), 맵 툴바 wrap+편집 컨트롤 "더보기" 수납, env 테이블 → 모바일 카드 reflow, 공유맵 패널 높이 제한(맵 터치영역 ≥55dvh 확보), Controls 모바일 top-right, 노드 풀커버 탭 타겟, 헤더 모바일 검색 버튼.
  - **③ 원클릭·템플릿(12건)**: 배포버튼 세로 스택, 카드 텍스트 오버플로우, 배포 진행 스크롤 캡, 모듈 DnD 터치 타겟 확대+PointerSensor 거리 상향, 모듈폼·에디터 탭 점프 해소.
  - **④ 인증·설정·폼(11건)**: OAuth 1열, Settings 모바일 가로탭 네비, MFA TOTP 입력 wrap, FAB safe-area, 피드백 필터/팝오버, 결제 다이얼로그.
  - **⑤ 가이드·카탈로그(9건)**: 통계그리드/메타그리드 반응형, 비교표 모바일 압축+스크롤 안내, 라이트박스 닫기 버튼 확대. **추가 발견**: `scrollbar-hide`가 전 코드베이스에서 무효(no-op) 클래스였음 → 실제 `scrollbar-none`+모바일 페이드 어포던스로 35개 가이드 파일 일괄 정상화.
  - 검증: typecheck(에러 0)·lint(에러 0, 신규 미사용 변수 1건 정리)·production build(webpack, exit 0, 전 라우트 컴파일) 통과. 모든 변경은 `md` 분기로 데스크톱 동작 보존.
- **인사이트:** 모바일 어색함 60여 건이 단 4개 안티패턴(고정 px / 데스크톱 오버레이 그대로 / hover 전용 / 터치 타겟 부족)으로 수렴 — 개별 땜질이 아니라 전역 토대(safe-area·overflow 가드·터치 토큰·바텀시트 패턴)를 먼저 깔고 재사용하는 게 일관성·재발방지의 핵심. 진단·구현을 5영역 병렬 에이전트로 분할하되 공통 토대 파일은 오케스트레이터가 직접 선점해 충돌을 차단. `scrollbar-hide`처럼 "있는 줄 알았던" 무효 클래스는 감사 과정에서만 드러나며, 모바일 가로 스크롤은 숨기기보다 어포던스(페이드) 노출이 정답.

### 2026-06-13 — 쇼케이스 관리자 모더레이션(타인 항목 내리기) 추가
- **카테고리:** #기능 #인프라
- **중요도:** P1
- **지시:** 쇼케이스의 작성자 외에도 관리자가 삭제(갤러리에서 내리기)할 수 있는 기능 추가.
- **맥락:** 쇼케이스 갤러리는 공개 노출 영역인데, 부적절한 항목을 내릴 권한이 작성자 본인에게만 있어 운영상 모더레이션이 불가능했음. 작성자 외 관리자도 내릴 수 있도록 권한 확장.
- **결과:**
  - **백엔드 권한 확장**: `api/oneclick/deployments/[id]/showcase`·`api/projects/[id]/showcase` PATCH의 소유권 게이트를 "소유자=모든 액션 / 관리자=unregister(모더레이션)만"으로 변경. register·update·toggle은 소유자 전용 유지(관리자가 타인 콘텐츠를 등록·수정하지 못하게).
  - **RLS 우회 처리**: 두 테이블 모두 UPDATE RLS가 `user_id = auth.uid()`라 사용자 클라이언트로는 관리자가 타인 행을 못 내림(0행 무음 실패). feedback 라우트와 동일 패턴(`admin ? createAdminClient() : supabase`)으로 관리자 unregister만 service role 우회. 코드 레벨 isAdmin 게이트 + 컬럼 범위 최소(is_showcase=false + 메타 초기화)로 방어.
  - **감사 로그**: 신규 액션 `showcase.admin_remove` 추가, 관리자가 타인 항목을 내릴 때 `{ owner_id, source }` 기록.
  - **UI**: 쇼케이스 상세(`/showcase/[id]`)에 삭제 버튼 신설 — 신규 `ShowcaseRemoveButton`(AlertDialog 확인). 제작자="쇼케이스에서 내리기", 관리자(비소유)="관리자 권한으로 내리기"(ShieldAlert 아이콘+모더레이션 안내). 본인 프로필 is_admin self-read로 클라 권한 판단. source별 올바른 엔드포인트 호출 후 갤러리로 이동·목록 무효화.
  - 검증: typecheck/lint 통과. RLS 정책은 Supabase에서 직접 조회로 확인.
- **인사이트:** "코드 레벨 소유권 체크"만 바꾸면 RLS가 조용히 막아 기능이 동작하지 않는다 — Supabase에서 권한 확장은 항상 RLS 정책과 함께 검토해야 하며, 관리자 교차 사용자 쓰기는 (RLS 정책 추가 vs service role 우회) 중 기존 패턴(feedback)을 따라 일관성 유지.

### 2026-06-13 — 무료 플랜 제한 점검 + 원클릭 배포 한도 표시·쿼터 정합성 보강
- **카테고리:** #기능 #인프라 #디자인
- **중요도:** P1
- **지시:** 무료 플랜 기본 적용 기능 제한과 원클릭 배포 갯수 제한 적용을 확인하고, 발견한 갭을 추가·반영.
- **맥락:** 원클릭 배포는 2층 퍼널 1층 진입점. 무료 한도(배포 3)는 코드/DB(`quota.ts`·`plan_quotas`·`create_homepage_deploy_atomic` RPC)로는 강제되지만 가격 페이지에 노출되지 않아 사용자가 차단 시점에서야 한도를 인지하는 마찰이 있었고, 원자적 RPC의 구독 상태 필터가 앱 레벨과 어긋나 있었음.
- **결과:**
  - **무료 제한 확인**: 프로젝트 3 / 환경변수 20·project / 서비스 10·project / 팀원 0 / **원클릭 배포 3** (Pro 10, Team 50, Admin 무제한). 배포 한도는 사전 체크(`checkHomepageDeployQuota`) + advisory lock 하 원자적 check-and-insert(`create_homepage_deploy_atomic`, M098~100)로 race까지 차단됨을 확인.
  - **Gap 1 — 가격 페이지 표시**: `pricing-content.tsx`에 플랜별 `deployLimit`(3/10/50) 필드 추가, 기능 목록 최상단에 "원클릭 배포 N개" 라인 렌더. i18n 동결 규칙 준수 위해 json 키 미추가 — 컴포넌트 내 양 로케일(ko/en) 리터럴로 처리.
  - **Gap 2 — RPC 쿼터 정합성(M101)**: `create_homepage_deploy_atomic`·`check_homepage_deploy_quota`·`check_project_quota`의 구독 필터를 `status = 'active'` → `status IN ('active','trialing')`로 정렬(앱 레벨 `quota.ts`의 `.in('status',['active','trialing'])`와 일치). trial 중 Pro/Team 사용자가 배포 RPC 경로에서 free 한도(3)로 잘못 차단되던 불일치 해소. 관리자 바이패스(M100)·enum 캐스트(M099)·권한 유지.
  - 검증: typecheck 통과. M101은 DB 적용 대기.
- **인사이트:** "한도를 강제하는 곳"과 "한도를 알리는 곳"이 분리되면 사용자는 차단당해서야 정책을 학습한다 — 진입 퍼널일수록 한도는 사전 노출이 마찰을 줄인다. 또한 동일 정책(구독 상태·관리자 바이패스)이 앱 레벨과 RPC 두 곳에 중복 구현되면 한쪽만 수정되어 드리프트가 생기므로, 쿼터 판정 기준은 단일 소스화가 바람직.

### 2026-06-12 — 환경변수/API 키 관리 심플 전면 개편 (검증=저장 후 옵션, 메모장형 UX)
- **카테고리:** #기능 #디자인
- **중요도:** P1
- **지시:** API 키/환경변수 관리를 메모장처럼 쉽게 입력·복사·전체 보기 가능하게, 키 검증은 저장 이후 옵션으로 분리, 형식 차이로 저장이 거부되지 않게 심플 우선 전면 개편 (보안은 유지).
- **맥락:** 서비스마다 키 형식이 달라 저장 시점 형식 검증이 초보자(바이브코더) 진입을 막는 마찰 지점이었음. "일단 저장은 무조건 성공 → 검증은 선택" 으로 철학 전환.
- **결과:**
  - **키 정규화(거부→자동 정리)**: 신규 `src/lib/utils/env-key.ts` (`normalizeEnvKey`/`ENV_KEY_REGEX`). Zod 스키마(`validations/env.ts`의 `envKeyNameSchema`)가 `transform`으로 정규화 — 소문자/특수문자/숫자 시작 키도 거부 없이 저장됨. create/update/bulk/raw 4개 경로 + `parse-env.ts` 파서 모두 동일 규칙 공유 (숫자 시작 키가 파싱에서 버려지던 문제도 해소).
  - **전체 보기/전체 복사**: `/api/env/decrypt`에 `{ ids[] }` 일괄 복호화 추가 (MFA 가드·소유권 필터·`env_var.bulk_decrypt` 감사 로그 유지, 최대 200개). 필터바에 "모든 값 표시/숨기기" 토글 + "전체를 .env 형식으로 복사" 버튼. 테이블에서 표시된 값은 줄바꿈 전체 표시 + 클릭 시 복사.
  - **검증=저장 후 옵션**: 저장 성공 토스트에 "지금 검증" 액션(서비스 연결 시), 행 메뉴에 "연결 검증 (선택)" — 기존 health-check API 재사용. 검증 실패해도 저장값 유지 안내.
  - **다이얼로그 심플화**: 추가/수정 모두 이름+값만 핵심 노출, 설명·서비스·환경·Secret은 "상세 설정" 접기. 값 입력 Input→Textarea (여러 줄 PEM 키 지원). 자동 서비스 감지 배지는 비침투 유지.
  - **보안 불변**: 암호화(AES-256-GCM)·MFA 가드·감사 로그·소유권 확인·NEXT_PUBLIC 경고 전부 유지. 복호화 실패 silent catch → 토스트로 교체.
  - 검증: typecheck/lint 통과, decrypt 테스트 12건(일괄 포함) 통과. 사전 실패였던 env decrypt/download 테스트(MFA mock 누락·영문 메시지 기대)도 수리. service-accounts 3건 실패는 본 작업과 무관(stash 대조 확인).
- **인사이트:** "형식 검증으로 입력을 막는 것"과 "보안"은 별개 축 — 검증을 빼도 암호화·감사·MFA는 그대로라 보안 저하 없음. 서버가 거부 대신 정규화(transform)하는 패턴은 클라이언트·API 직접 호출 모두에서 입력 실패율을 0에 수렴시킴.

### 2026-06-11 — 원클릭 배포 안정화·하드닝 (빌드 재현성 + 엔드포인트 강건화)
- **카테고리:** #인프라 #버그 #리팩터
- **중요도:** P1
- **지시:** 원클릭 배포의 배포→점검→재배포 전 과정을 디테일하게 점검·분석하여 문제를 도출하고, 안정화 및 안정성 보강 기획·구현.
- **맥락:** 원클릭 배포는 2층 퍼널의 1층 진입점(바이브코더 첫 경험)인데, "오늘 되던 배포가 내일 깨진다"류의 재현 불가능·관측 곤란 문제가 잠재. 코드 직접 검증으로 실제 이슈와 오탐(고아 프로젝트·무한 폴링 2건)을 분리한 뒤 구조적으로 보강.
- **결과:**
  - **WS1 빌드 재현성(P0)**: 템플릿 번들에 lockfile 부재 + `deploy.yml`의 `npm install` → transitive 부유로 빌드 비결정성. 의존성 세트별 lockfile 2종(standard/namecard) 생성(`scripts/gen-template-lockfiles.mjs`, `src/data/oneclick/locks/`), `makePackageLock()` 헬퍼로 8개 템플릿 번들에 `package-lock.json` 주입, `deploy.yml`을 `npm ci`로 전환. 드리프트 가드 테스트(`template-deps.test.ts` 16건)와 `npm ci` 실증 검증 스크립트(`verify-template-lockfiles.mjs`) 추가. 기존 배포 레포는 deploy.yml 동결로 무영향(무중단).
  - **WS2 엔드포인트 하드닝(P0/P1)**: `deploy/route.ts` 리소스 생성 순서를 GitHub(레포·Pages·푸시) 먼저 → DB(프로젝트·SA·배포레코드) 마지막으로 재배치 → 중도 실패/요청 중단 시 고아 DB row 미발생. 이름충돌 재시도 31→8회 축소 + 150~400ms 지터 백오프 + 20s 누적 예산(게이트웨이 타임아웃·GitHub 2차 rate-limit 회피).
  - **WS3 구조화 에러 코드(P1)**: `apiErrorWithCode` 추가, 배포 실패 응답에 안정적 `code`(=로깅 카테고리) 포함, 클라 `deploy-error-map`이 문구 substring 대신 code 우선 매핑.
  - **WS4 타임아웃·폴링(P1)**: `deploy-status.ts` 타임아웃을 **GitHub 실제 상태 우선**으로 재구성 — 상태를 확인한 뒤 여전히 building일 때만 `updated_at` 기준 15분 무진전으로 에러 판정(혼잡한 러너로 15분 넘겨 성공한 빌드를 잘못 실패시키지 않음). `useDeployStatus` 하드 폴 캡·연속 실패 캡 추가. (※ 초기 검토 시 추가했던 `created_at` 절대 상한은 "60분 이상 된 배포 재배포 시 즉시 타임아웃" 버그라 제거.)
  - **WS5 무결성(P2)**: `validateModuleState`에 minLength/maxLength·number min/max·email·url(관대)·maxItems 검증 활성화. `template-integrity.test.ts`에 생성 코드 구문 검증 + default 상태 검증 안전성 추가. 안전성 테스트가 link-card `emoji` 필드(아이콘 키를 담는데 maxLength:4) 사전 불일치를 적발→교정.
  - 검증: typecheck/lint(에러 0) 통과, oneclick 테스트 68건 통과. env/mfa 관련 사전 실패 15건은 본 작업과 무관(stash 대조 확인).
  - **범용성 검증(임의 로그인 유저 관점)**: 병렬 감사 2건으로 (a) 계정/스코프/워크플로우 범용성 — OAuth 스코프(repo/workflow) 적절, `createRepo` public 기본(무료 계정 Pages 가능), `/user/repos`로 항상 개인 계정 생성, Actions 워크플로우 요건(permissions/build_type:workflow) 충족 확인. (b) 재배치된 deploy 흐름·이름채번·폴링·에러코드·재배포 정합성 확인. 발견된 실제 이슈를 추가 수정: ① 타임아웃 상태우선 재구성(위), ② **배포 전 workflow 스코프 사전 검증** 추가(좁은 스코프 계정의 고아 레포+늦은 403 방지), ③ **배포 quota 원자화** — migration 052의 `check_homepage_deploy_quota`는 count-only라 check↔INSERT가 별도 트랜잭션이면 advisory lock이 먼저 해제돼 동시 배포 시 한도 초과 가능했음. migration 098 `create_homepage_deploy_atomic` RPC로 advisory lock 하의 단일 트랜잭션에서 count+INSERT를 묶어 진짜 원자성 확보(SECURITY DEFINER+auth.uid()+프로젝트 소유권 검증). deploy route의 최종 insert를 이 RPC로 교체, 한도 초과 시 GitHub 레포 정리 후 quota 에러. 미해결 항목(후속): GitHub 토큰 refresh 미구현(현 OAuth App은 만료 없어 무영향).
  - **빌드 범용성 실증**: 8개 템플릿 번들을 디스크로 추출해 GitHub Actions와 동일한 `npm ci && next build`(static export)를 **WSL/Linux(=ubuntu)** 에서 실행해 산출물 빌드 검증(`scripts/materialize-template.ts`, `.bt/build-wsl.sh`). Windows 로컬은 `@vercel/og` `fileURLToPath` 이슈로 OG 라우트 프리렌더 불가(환경 한정, ubuntu 무관) → Linux 검증으로 우회.
- **인사이트:** Explore 보고의 P0 주장 2건(고아 프로젝트·무한 폴링)이 코드 직접 검증 결과 오탐으로 판명 — 에이전트 발췌 보고는 반드시 원본 검증 후 채택. lockfile은 의존성 세트가 다르면(`qrcode.react`) 단일 공유 불가, 세트별 관리 필요. "default 상태는 형식 검증을 통과해야 한다"는 불변식 테스트가 검증 강화의 안전망이자 잠재 스키마 버그 탐지기로 동작.

### 2026-06-11 — 가이드 전체 최신화 · 초보자 고도화 · 신규 3종 추가
- **카테고리:** #콘텐츠 #기능 #리팩터
- **중요도:** P1
- **지시:** 서비스 내 가이드 전체를 현시점(2026-06)에 맞게 최신화하고, 초보자 타겟에 맞춰 고도화 + 확장 항목 추가, 문서화 병행.
- **맥락:** 2층 퍼널의 "성장" 단계 핵심 자산인 가이드가 ① 시간민감 콘텐츠(AI 모델/가격/버전) 노후화, ② 가이드별 초보자 친화도 편차 문제가 있었음. 1순위 타겟인 바이브코더(코딩 경험 0) 진입장벽을 낮추는 것이 목표.
- **결과:**
  - **공통 컴포넌트 키트 신설** (`src/components/guides/common/`): `GuideTLDR`·`GuideCallout`·`CommonMistakes`·`AnalogyBox`·`FaqSection`·`GlossarySection`. 기존 auth/env의 중복 FAQ·용어사전을 공통화로 흡수(중복 제거).
  - **최신화**: AI 모델 비교(`models-content.tsx`·`ai-models-section.tsx`·`ai-api-content.tsx`·OpenAI 가이드) — Claude Opus 4.8 + Fable 5, GPT-5.5/5.4, Gemini 3.1 Pro/3.5 Flash, DeepSeek V4로 갱신. 공식 가격표 기준 검증, `lastUpdated` 2026-06-11. 검증 불가 수치(코딩 1위·5.5배 등) 완화. Node 18→20 권장.
  - **초보자 고도화(TL;DR 전수)**: 메인 가이드 23개 전부에 hero 직하 `GuideTLDR`(핵심 3줄 + 이걸 배우면 뭘 할 수 있나 + 난이도/소요시간) 삽입.
  - **신규 가이드 3종**: `흔한 에러 해결 허브`(최상위 `/guides/troubleshooting`), `배포 후 첫 점검`(`/guides/deploy/post-deploy-checklist`, 인터랙티브 체크리스트), `AI 비용 관리·토큰 절약`(`/guides/ai-basics/cost-saving`). `guide-meta.ts`/`guide-data.ts` 메타·학습단계·러닝패스 배선, `warm-cache.sh` 경로 추가.
  - **문서화**: `docs/guides/guide-content-audit-2026-06.md`(전수 점검표), `docs/guides/guide-enhancement-pattern.md`(공통 키트 사용법·표준 템플릿).
  - 검증: `typecheck` 통과, `lint` 신규 파일 에러 0(기존 env 체크리스트와 동일한 set-state warning 1건만).
- **인사이트:** TL;DR을 메인 가이드 `*-guide.tsx`의 단일 지점(hero 직하)에 삽입하는 방식으로 23개 가이드를 일관되게 고도화. 공통 컴포넌트를 데이터 주입형으로 설계해, 향후 가이드 추가 시 4종 고도화(TL;DR·비유·흔한 실수·용어/FAQ)를 복붙 없이 적용 가능. AI 모델 콘텐츠는 변동이 잦아 분기별 재검증 루틴이 필요.

### 2026-06-12 — 원클릭 배포 E2E 흐름 테스트 체계 신설 + 프로덕션 배포 차단 결함 발견·핫픽스
- **카테고리:** #QA #버그 #기획
- **중요도:** P0
- **지시:** 원클릭 배포를 2개 페르소나(신규 GitHub 미보유 초급자 / 기존 GitHub 연동 사용자)로 기획하고, 시작→배포→모듈 수정→라이브 반영까지 프로덕션에서 실제 테스트. 재사용 가능한 문서 체계로 정리.
- **맥락:** 06-11 안정화 작업의 실사용 경로 실증이 없었음. Playwright MCP 기반 프로덕션 E2E를 수행하고, 이후 반복 가능한 흐름 테스트 방법론을 `docs/e2e-flows/`로 표준화.
- **결과:**
  - **문서 체계 신설**: `docs/e2e-flows/` — README(방법론: CP 핸드오프·백오프 대기·마커 검증·자격증명 비기록) + `_templates/`(시나리오·리포트) + `2026-06-12-oneclick-deploy/`(시나리오 + 케이스별 리포트 + 스크린샷 26장).
  - **P0 결함 발견·핫픽스(B-2)**: migration 098 RPC의 `plan_quotas.plan`(enum) = TEXT 비교 → `operator does not exist: subscription_plan = text` → **06-11부터 모든 사용자 배포 전면 500**(고아 repo `mypage-bm`·`mypage-4l`이 증거). → **migration 099** `::subscription_plan` 캐스트(052의 잠재 동일 버그 2개 함수 포함) 적용.
  - **HIGH 결함 발견·핫픽스(B-6)**: 동일 RPC에 `profiles.is_admin` 바이패스 누락 — 앱 레벨 quota.ts와 정책 불일치로 관리자 403(43/3). B-2가 가리고 있던 버그. → **migration 100** admin bypass 추가.
  - **Case B(기존 사용자) PASS**: 단축 경로(게이트 미발동)·이름충돌 자동 suffix(-vg)·빌드 95초·모듈 수정→라이브 반영 2분·원자 커밋 검증.
  - **Case A(신규 초급자) PASS**: 이메일 가입→GitHub 게이트 모달→보조 계정 OAuth→**왕복 후 위저드 상태 보존("배포 진행")**→배포 201(free 쿼터 1/3 정상)→빌드 100초→모듈 수정→마커 라이브 반영 3분. 가입→라이브까지 약 12분.
  - **미수정 발견사항(별도 작업)**: B-3 cleanup의 deleteRepo가 `delete_repo` 스코프 부재로 403 silent→고아 repo 누적(HIGH) / B-1 preflight raw fetch User-Agent 누락→사이트명 중복 거짓 경고(WARNING) / A-4 GitHub 미보유자 가입 안내 부재 + 랜딩 "GitHub 가입 자동" 문구 불일치(WARNING, 1순위 타겟 직결) / B-4·B-5 에러 화면 카피·재시도 폼 초기화 / A-2 만료 인증링크 안내 부재.
- **인사이트:** ① PL/pgSQL의 enum=TEXT 비교는 리터럴(`'free'`)은 통과하지만 변수 경유(`COALESCE(v_plan,...)`)는 42883으로 죽는다 — RPC를 크리티컬 패스에 넣을 땐 프로덕션 스키마 타입으로 실행 검증이 필수. ② 사전 체크(quota.ts)와 DB RPC의 정책 이중화는 드리프트를 낳는다(admin bypass 한쪽 누락). ③ E2E가 아니면 못 잡는 버그였음 — 단위 테스트는 mock DB라 enum 타입 불일치를 통과시킴. 흐름 테스트 체계(e2e-flows)의 존재 가치 실증.
