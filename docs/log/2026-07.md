# 2026-07 작업 로그

### 2026-07-12 — 카카오·구글 로그인 서비스 가이드 Supabase 연동 기준 v2 보강
- **카테고리:** #콘텐츠 #가이드 #인증
- **중요도:** P1
- **지시:** "서비스가이드에 로그인 연결을 만들고 카카오 로그인과 구글로그인 연결방법을 supabase 연결기준으로 세팅 디테일내용과 설명을 기획해서 업데이트 적용해줘" — 적용 위치는 DB 서비스 가이드(ServiceGuideSeed), 범위는 카카오+구글로 확정.
- **결과:**
  - **기술 사실 교정(핵심):** 기존 카카오 가이드의 "Supabase Custom OIDC Provider(Issuer URL: kauth.kakao.com)" 안내는 구식 — 현재 Kakao는 Supabase **네이티브 Provider**(Authentication > Sign In / Providers > Kakao, REST API 키 + Client Secret 코드). 구글도 2024년 콘솔 개편(**Google Auth Platform**: Branding/Audience/Clients/Data Access) 반영. 공식 문서(supabase.com/docs auth-kakao·auth-google, Google support) 웹 검증 후 작성.
  - **kakao_login 엔트리 전면 재작성** (`src/data/seed/service-guides.ts`): setup_steps 6→9단계(플랫폼 키 신 UI 경로·Redirect URI·동의항목·URL Configuration·프로덕션 체크리스트), code_examples 3개(@supabase/ssr 기준 signInWithOAuth·callback_route·user_metadata, 구식 direct_oauth 삭제), signup 가입 안내, features 3개(kakao-provider/consent-email-bizapp/nextjs-callback — issue_steps 포함), pitfalls 3→6개(KOE006·KOE101·구버전 OIDC 가이드 함정·localhost 리다이렉트·PKCE 콜백 누락 등).
  - **google_oauth 엔트리 전면 재작성**: setup_steps 4→8단계(Auth Platform 4개 메뉴 흐름·테스트 100명 한도·Publish app), code_examples 2개, signup, features 3개(oauth-client/consent-screen/nextjs-callback+provider_token), pitfalls 3→6개(refresh token null·메뉴 개편 혼란 등), api_key_url을 신 UI(console.cloud.google.com/auth/clients)로 갱신.
  - **cross-reference:** 양 가이드에 supabase 팁 리라이트 + 상호 팁(멀티 프로바이더 automatic identity linking 주의) 추가, supabase 엔트리 integration_tips에 kakao-login/google-oauth 역방향 팁 2개 추가(4→6개).
  - **DB 반영 SQL 생성:** `docs/db-uploads/2026-07-12_service_guides_login_supabase.sql` (tsx 스크립트로 시드에서 자동 추출 — kakao/google 전 컬럼 upsert + supabase integration_tips만 UPDATE, 달러 쿼팅).
  - 검증: typecheck 0 errors · 병합 serviceGuides 97개·중복 service_id 없음 · SQL 달러 태그 충돌 검사 통과.
- **인사이트:** `/services/[slug]` 렌더러(service-detail-client.tsx:318)는 `features`가 있으면 최상위 setup_steps/code_examples/api_key_url 카드를 렌더하지 않는 fallback 구조 — v2 보강 시 화면에 보여야 할 흐름은 signup+features 안에 담고, 최상위 필드는 DB 규격·타 소비자용으로 병행 유지해야 한다. 페이지가 `revalidate=false` 정적이라 SQL 반영 후 재배포+캐시 워밍업 필요.

### 2026-07-12 — 로그인 세션 자가치유: 키 로테이션 후 좀비 세션 전면 401·재로그인 불가 해소
- **카테고리:** #버그 #보안 #인증
- **중요도:** P0
- **지시:** "로그인이 되어있는데 끊기면서 재로그인이 안 된다" — 로그인 화면 콘솔에 Supabase REST `/rest/v1/services` 401 반복. 원인 진단 + 로그인이 끊기지 않도록 검토·수정.
- **진단:**
  - Supabase 키 로테이션(6-21 P0 인시던트 대응, 레거시 JWT → `sb_publishable_`) **완료 확인** — 로컬 `.env.local`·라이브 번들(`linkmap.biz` 청크) 모두 신형 키로 일치, REST/Auth 엔드포인트 신형 키로 200 정상.
  - 문제는 **로테이션 이전에 로그인한 브라우저의 세션 쿠키(구 JWT 서명)가 좀비로 잔존** — 무효 토큰이 Authorization으로 계속 전송돼 전면 401(로그인 화면의 401은 전역 CommandPalette의 `useCatalogServices`가 발사). 미들웨어·클라이언트 어디에도 죽은 세션 정리 로직이 없어 자가복구 불가, 재로그인도 오염된 쿠키 상태에서 불안정.
  - 부가 발견: 미들웨어가 리다이렉트 시 갱신된 세션 쿠키를 유실 → 브라우저가 이미 소비된 refresh token 재사용 → Supabase 재사용 감지로 세션 강제 종료(간헐 로그아웃의 또 다른 원인).
- **결과:**
  - **`src/lib/supabase/auth-recovery.ts` 신설**: `isDefinitiveAuthFailure()`(GoTrue 4xx·PostgREST PGRST30x·JWT 메시지·세션 파싱 불가 → true, 네트워크 일시 오류 → false) + `isAuthSessionCookie()`(PKCE code-verifier 제외).
  - **`session.ts`(미들웨어)**: 인증 쿠키 존재 + 확정적 인증 실패 시 `sb-*-auth-token*` 쿠키 자동 만료(자가치유) — 다음 요청부터 깨끗한 비로그인 상태 복구. 리다이렉트 응답에 회전된 토큰 쿠키 전달(`copyResponseCookies`). silent catch 제거(console.error + fail-open).
  - **`providers.tsx`**: QueryCache/MutationCache 전역 onError로 확정 인증 실패 시 local signOut + 보호 경로면 `/login` 전체 리로드(미들웨어 경유로 서버측 쿠키 정리 보장). 초기 마운트 시 세션 존재하면 `getUser()` 1회 서버 검증 → 무효면 즉시 정리. retry 가드에 `isDefinitiveAuthFailure` 추가(기존 `message.includes('401')`은 PostgrestError "JWT expired"를 놓침).
  - 검증: 신규 테스트 20개(auth-recovery 11 + session 9) 포함 전체 vitest 260/260 통과 · typecheck 0 errors · 변경 파일 lint clean.
- **인사이트:** 키 로테이션은 "서버 측 완료"가 끝이 아니라 **기존 사용자 브라우저의 좀비 세션 정리까지가 마이그레이션** — 미들웨어에 확정적 인증 실패 → 쿠키 만료 경로가 없으면 로테이션마다 전 사용자가 수동 쿠키 삭제를 해야 한다. 콘솔의 CSP connect-src 위반은 Report-Only(차단 없음)로 본 건과 무관.

### 2026-07-12 — 카페 원클릭 고도화: 네이버 플레이스 실데이터(카페 라이츠) 기본값 교체 + naverPlaceUrl 필드
- **카테고리:** #기능 #콘텐츠
- **중요도:** P1
- **지시:** 중단됐던 "네이버 플레이스 기반 카페 원클릭 고도화" 작업을 재검토 후 순서대로 전체 진행 (문서 4건 + 코드 2건).
- **맥락:** 카페 템플릿(small-biz-cafe)의 가상 데모 데이터 "온기 로스터리"를 실존 카페 "카페 라이츠"(건대입구, 네이버 place 2007976823) 실데이터로 교체해 쇼케이스 신뢰도를 높이는 Phase 1 실증. AI API·별도 비용 없이 결정적 파싱/치환만 사용 (`docs/onelink/naver-cafe/` 기획 문서 참조). 문서(01~04·README)는 선행 세션이 완성, 코드 반영이 미착수 상태였음.
- **결과:**
  - **문서 확인**: `docs/onelink/naver-cafe/` 01-feasibility·02-service-plan·03-prompts·04-cafe-wrights-dataset + README 5종 완비 확인 (03·04는 선행 세션 작성분 검수 후 채택).
  - **템플릿 기본값 교체(4파일)**: `module-schemas/small-biz-cafe.ts`(hero/about/menu 14종/hours/location/sns defaultValue), `generators/small-biz-cafe.ts`(기본값 7종), `small-biz-cafe-template.ts`(siteConfig·DEMO_MENU 14종·DEMO_HOURS 7행·About/메뉴/갤러리/SNS 섹션 카피·TAB_EMOJIS 신규 카테고리), `template-sample-content.ts`(smallBizCafeSample.ko). 메뉴 카테고리 시그니처/커피/티·라떼/에이드 재분류, 대표 메뉴 `isPopular`, 월간 페어링 `isNew`. 디자인 토큰(primaryColor/font)·businessNumber 가상값·갤러리 placeholder는 정책대로 유지(네이버 이미지 미사용).
  - **naverPlaceUrl 신규 필드**: location 모듈 스키마 + 템플릿 siteConfig(`NEXT_PUBLIC_NAVER_PLACE_URL`) + info-section 네이버지도 버튼(플레이스 URL 우선, 없으면 주소 검색 fallback) + `base-generator.ts` generateConfigTs/parseConfigToState 배선 — 생성 config에 필드가 누락되면 배포 사이트 빌드가 깨지므로 제너레이터 배선 필수였음.
  - **템플릿 피커 노출 순서**: `template-picker-step.tsx`의 상단 고정 슬롯을 link-card → small-biz-cafe로 교체. display_order(카페 6·링크카드 7)는 카탈로그/랜딩에서 이미 카페가 앞서므로 무변경 — "카페↔링크카드 자리 교체"의 실체는 피커 고정 슬롯이었음.
  - 검증: `typecheck` 0 errors · `lint` 0 errors(경고는 기존분) · vitest 전체 240/240 통과 · src 내 "온기/Ongi" 잔존 0건.
- **인사이트:** 카탈로그는 파일 기반 단일 소스(`src/data/templates/index.ts`)라 DB 반영 불필요. 노출 순서류 요청은 display_order 외에 컴포넌트 하드코딩 정렬(고정 슬롯)이 있는지 먼저 확인할 것. 이후 카페 온보딩·갱신은 `docs/onelink/naver-cafe/03-prompts.md` P-1/P-2 프롬프트로 반복 실행.

### 2026-07-12 — 모바일 초대장 템플릿 2026 트렌드 프리미엄 리디자인 (멀티에이전트)
- **카테고리:** #디자인 #원클릭 #기능
- **중요도:** P1
- **지시:** "원클릭배포의 에이전트와 서브에이전트 활용해서 웹분석을 통해 모바일초대장을 최신트렌드의 초대장모습으로 디자인 업데이트 — 실제 초대장으로 활용 가능한 고퀄리티, 모듈편집·기능 무결성 유지, 심플하면서 고급스럽게."
- **파이프라인:** design-director(2026 트렌드 웹 분석: Paperless Post/Paperlust/Minted/바른손·보자기카드 등) + Explore(코드 구조 감사) 병렬 → 원클릭 오케스트레이터 → tpl-invitation 구현 → design-qa-reviewer 검수(G1~G8) → AA 대비 FAIL 2건 수정 라운드 → 메인 세션 최종 검증.
- **결과:**
  - **컬러 시스템**: 프리셋 6종(ID 불변 — 기존 저장 상태 호환) 팔레트를 웜 뉴트럴 + 단일 딥 액센트 기조로 전면 재정의. `InvitationPresetVars`에 `accentSolid`(흰 글자 버튼용 고대비, AA 4.5:1 검증값) 추가, `accentSoft`/`accentGlow`는 accent에서 파생(hexToRgba) — **--inv-accent-soft 골드 하드코딩 누수 버그 제거**.
  - **타이포/구성**: eyebrow 트래킹 라벨(.16em uppercase), Nanum Myeongjo 세리프 디스플레이 스케일, tabular-nums(디데이·계좌), 헤어라인 디바이더(중앙 ✦), 모노그램 북엔드(히어로↔푸터). hero radial 베인닛+SVG 노이즈+아치형 clip-path 프레임, dday 월 캘린더 그리드 위젯 신설, location/account/share 네이티브 `<details>` 아코디언+원탭 복사, gallery 비대칭 편집 레이아웃, footer safe-area.
  - **모션**: animated-reveal을 프로그레시브 방식으로(기본 opacity:1 — 카톡 인앱 WebView에서 JS 실패해도 콘텐츠 보장), IntersectionObserver+transform/opacity만, reduced-motion 시 Observer 미부착+CSS 전역 무력화.
  - **기능 버그 수정**: `hero.fontFamily` 배선 완결(generateConfigTs → generatePageTsx @import+`--inv-font-*` 주입 → globals.css 소비 → parseConfigToState 왕복) — 기존엔 프리뷰만 반영되고 배포 산출물 무반영이던 괴리 해소.
  - **3중 동기화**: generator `INVITATION_PRESET_THEME` ↔ preview `PRESET_VARS` ↔ 배포 globals.css `:root` 동일값. 글래스는 minimal-glass 한정(히어로+D-day) + `@supports not (backdrop-filter)` 솔리드 폴백 양쪽 CSS 포함.
  - 변경 5파일: module-schemas/module-presets/invitation-template(±3,018줄)/generators/preview. 검증: typecheck 0 errors · template-integrity+deps 76/76 · 프리셋ID·모듈ID·필드key 전부 불변(모듈 편집기 무영향) · 신규 의존성 0.
- **인사이트:** invitation은 배포 마크업(invitation-template.ts 인라인 문자열)과 프리뷰 HTML(preview/invitation.ts)이 별도 구현 + 팔레트가 3곳 분산 정의라, 디자인 변경은 반드시 "3중 동기화 + 마크업 2벌" 세트로 움직여야 프리뷰≠배포 괴리가 안 생긴다. 프리셋 ID는 저장 상태의 designPreset 폴백(elegant-gold) 때문에 사실상 공개 API — 리브랜딩은 라벨로만. 잔여(비차단): romantic-pink/modern-minimal/minimal-glass 3종의 gradientFrom/To가 accentSolid와 미묘한 톤 차 — 후속 조정 가능.

### 2026-07-13 — services 401 재신고 진단: 스테일 엣지 캐시 갭 발견 + 배포 캐시 퍼지 단계 추가
- **카테고리:** #버그 #보안 #인프라
- **중요도:** P1
- **지시:** 라이브 콘솔의 `GET /rest/v1/services 401 (Unauthorized)` 로그인 오류 확인 요청 (07-12 자가치유 커밋 이후 재신고).
- **진단 (라이브 직접 검증):**
  - 현재 라이브는 정상 — 번들 인라인 키 `sb_publishable_Du-k...V`로 REST anon services 조회 200, Auth health/settings 200(Google/GitHub/Kakao 활성), services RLS는 migration 049에서 anon 읽기 허용, 자가치유(d46a855c)가 layout 청크에 포함됨.
  - **핵심 발견 — 스테일 엣지 캐시**: HTML `s-maxage=31536000`(1년) + 배포 파이프라인에 캐시 퍼지 부재로, 자가치유 커밋(07-12 11:44) 후에도 **~12시간 동안 구 빌드 HTML이 서빙**됨(구 layout 청크는 404, 캐시 우회 `/?cb=` 요청과 빌드 해시 불일치로 실증). 구 번들에는 폐기된 키(오전 `JM-e6...` → 재발급 `Du-k...V`)가 인라인 → 익명 포함 전원 401 + 로그인 불가. `warm-cache.sh`는 GET만 수행하므로 캐시 HIT이면 갱신 안 됨 — 워밍이 퍼지를 대체하지 못함.
  - 신고된 401의 나머지 경로는 좀비 세션 쿠키(구 JWT) — 배포된 자가치유가 401을 트리거로 세션을 정리하므로 **콘솔 1회성 401은 정상 잔흔**(오진 주의).
- **결과:**
  - `deploy-cloudflare.yml`에 **Purge Cloudflare cache 단계 추가** (`wrangler deploy` 직후, warm-cache 직전): zone `purge_everything` API 호출, `CLOUDFLARE_ZONE_ID` secret 미설정 시 경고 후 스킵(continue-on-error), 직후 warm-cache가 재워밍하므로 콜드 스파이크 완화.
  - 메모리 갱신: security-incident(키 재발급·자가치유 라이브 검증·2차 후폭풍), deploy-pipeline-ops(warm-cache≠퍼지, 스테일 판별법, `curl --compressed` 필수).
  - **잔여(사용자 수동)**: GitHub Secret `CLOUDFLARE_ZONE_ID` 추가 + `CLOUDFLARE_API_TOKEN`에 Zone > Cache Purge 권한 부여 — 미설정 시 퍼지 단계는 경고만 내고 스킵됨.
- **인사이트:** 키 로테이션의 완결 조건은 "서버 키 교체 + 좀비 세션 정리 + **엣지 캐시 퍼지**" 3종 세트 — 번들에 키가 인라인되는 구조(NEXT_PUBLIC_*)에서는 캐시된 구 HTML이 폐기 키를 무기한 서빙할 수 있다. 라이브 번들 포렌식 시 `curl --compressed` 없이는 압축 바이너리를 grep해 "키 없음" 오탐이 나며, RSC flight 참조 청크(`static/chunks/...` 무접두사)까지 훑어야 layout 청크가 잡힌다.

### 2026-07-13 — 모바일 초대장 2차 전면개편 "라이트 & 에어리 럭셔리" + 배포 반영 검증 체계
- **카테고리:** #디자인 #원클릭
- **중요도:** P1
- **지시:** ① "실제 원클릭배포 반영 안 된 것으로 보임" 검토 → ② "색상과 디자인 개선해서 전체적인 분위기가 고급스럽게 전면개편".
- **①검증 결과:** 신규 배포에는 1차 리디자인 정상 반영 확인(myinvite-6i 라이브 Playwright 실측 — 캘린더 위젯·모노그램·리빌 동작). 단 라이브 실물에서 품질 문제 발견: elegant-gold 히어로가 탁한 머스터드 풀블리드 블록, 🎉 이모지+두꺼운 골드 보더 카드, 과거 기본 날짜(2026-06-15)로 첫 화면부터 "행사 종료" 상태. **구조 갭 발견**: 기존 배포 사이트는 모듈 "적용" 시 config.ts+page.tsx만 푸시되어 컴포넌트·globals.css 디자인 업데이트가 영구 미전달(개선 백로그: "템플릿 디자인 최신화" 재동기화 기능).
- **②결과 (원클릭 오케스트레이터 → tpl-invitation → design-qa-reviewer 1회 수정 라운드):**
  - **히어로 컨셉 전환**: 다크 그라디언트 풀블리드 → 라이트 아이보리 캔버스 + gradientFrom/To를 저투명 radial 글로우(16%/10% color-mix)로만 사용 + 플로팅 오브. 텍스트 흰색→잉크 전환, 모노그램 순수 타이포(이모지 링 제거).
  - **전역 톤**: 섹션 여백 확대(--inv-section-py 변수화), 두꺼운 accent 보더 카드 금지(1px 헤어라인), accent는 선·점에만(면 CTA는 rsvp 1개), 디바이더 opacity .28, 장식 이모지 전량 제거(🎉🚗🚏📋 등 → 인라인 SVG/타이포 마크).
  - **디데이**: 기본 eventDate 미래 날짜(2026-12-19)로 교체(스키마+프리셋+placeholder 동기화) — 첫 배포 화면에 캘린더 하이라이트+카운트다운 라이브. 프리셋 6종 gradientFrom/To 라이트 틴트 전면 교체(1차 잔여 톤 부조화 해소).
  - QA 1차 불합격(Blocker 3: 프리뷰 히어로가 구 다크 코드 잔존 등) → 수정 라운드로 전 항목 해소, 프리뷰↔배포 마크업 2벌 동기화 복구.
  - 검증: typecheck 0 · integrity+deps 76/76 · **materialize→npm ci→next build 정적 export 성공** · 로컬 서빙 Playwright 375px 실물 스크린샷으로 프리미엄 룩 확인 후 커밋.
- **인사이트:** 원클릭 템플릿 디자인 검증은 코드 리뷰·프리뷰만으로 부족 — `scripts/materialize-template.ts`(invitation은 OG 라우트가 없어 Windows 빌드 가능) → 로컬 정적 서빙 → Playwright 실측이 배포 전 최종 게이트로 유효. 배포 후에는 신규 사이트를 만들어야 새 디자인이 보인다(기존 사이트 미전달 갭).

### 2026-07-13 — 모바일 초대장 실사용 샘플 콘텐츠 적용 (웹 이미지 소싱 + 웨딩 시나리오)
- **카테고리:** #콘텐츠 #원클릭
- **중요도:** P1
- **지시:** "실제 샘플링이미지를 웹에서 찾아서 실제 활용하는 자료처럼 디테일 꾸며서 업데이트 적용".
- **결과:**
  - **이미지 소싱**: Unsplash(무료 상업 라이선스)에서 Playwright로 후보 27장 수집 → 320px 컨택트 시트로 시각 검수 → 라이트&에어리 팔레트에 맞는 7장 선정(히어로: 뮤트 베이지 포옹 컷 — 얼굴 비노출, 갤러리 6장: 부케·반지·커플). 전 URL HTTP 200 검증 후 채택.
  - **웨딩 샘플 시나리오**(전부 가상): 김민준·이서연, 2026-12-19(토) 14시, 라온제나 홀 3F, 격식 인사말, 신랑측/신부측 가상 계좌·연락처, 교통/주차 안내.
  - **적용 5파일**(tpl-invitation 위임): module-schemas defaultValue 전 모듈, generator `??` 폴백 5곳 동기화, invitation-template placeholder config.ts 재작성, template-sample-content invitationSample, preview eventType 폴백 'gathering'→'wedding' 잔존 수정. `gallery.defaultEnabled false→true`(갤러리 6장 기본 노출 — 유일한 플래그 변경).
  - 검증: typecheck 0 · integrity+deps 76/76 · materialize→빌드→로컬 Playwright 실측(이미지 7/7 로드·전 섹션 리빌·구 샘플 문구 0건) → myinvite-6i 반영.
- **인사이트:** 리빌 검증 시 프로그램적 순간 점프 스크롤은 하이드레이션·이미지 디코딩과 경합해 pending 잔존으로 오탐된다 — 300px/350ms 점진 스크롤 + 체류로 검증해야 실사용과 등가. 기본 샘플은 스키마 defaultValue·generator 폴백·template placeholder config 3곳이 같은 값을 봐야 하며(integrity 테스트가 구문만 검증, 값 일치는 미검증), 다단락 문구는 `\\n` 이스케이프 관례(dev-showcase 참조) 준수.

### 2026-07-13 — 디지털 명함 고도화 P0+P1 (에이전트 정비 → 프리셋 완성형 룩 → share/vCard/tagline/다크모드)
- **카테고리:** #원클릭 #에이전트
- **중요도:** P1
- **지시:** "디지털명함 에이전트 확인해서 고도화 기획진행" → "해당기획 문서화하고 업데이트진행".
- **진단 (기획: `docs/planning/2026-07-13_digital-namecard-upgrade.md`):** 7개 템플릿 중 최소 구성(모듈 4개). **구조 결함 발견**: `tpl-digital-namecard.md` 담당 파일에 배포 번들(`homepage-template-content.ts` — 카드 UI 11개 컴포넌트의 실소스)이 누락되어 에이전트가 디자인을 수정할 수 없는 상태(제너레이터는 config.ts+page.tsx 껍데기만 생성). 추가로 `preset-override.css`의 `:root` 토큰이 globals.css `.dark`와 동일 특이도로 나중 로드되어 모듈 편집 후 다크모드 토글 무력화 버그 후보 확인.
- **결과:**
  - **P0-1 에이전트 문서 보강**: 담당 파일에 번들 구간(802~1830줄) 추가 + 컴포넌트 맵 11개·template literal 이스케이프 규칙·i18n 양언어 동시 추가·config 3곳(번들/generateConfigTs/parseConfigToState) 동기화·케스케이드 주의사항 명문화.
  - **P0-2 프리셋 완성형 룩**: accentColor 단독 → `designPreset+accentColor+fontFamily` 묶음 5종(ID 하위호환 유지). midnight는 minimal-dark+연한 인디고(#818cf8 — 다크 배경 가독성), warm-earth는 세리프, creative는 IBM Plex.
  - **P1 (tpl-digital-namecard 위임 + 메인 세션 마무리)**: ① contact.enableShare + ShareButton(Web Share API/클립보드 폴백, 보조 버튼 스타일) 신설 ② vCard 3.0 보강(X-SOCIALPROFILE·extraContacts type별 EMAIL/TEL/URL/NOTE·PHOTO;VALUE=URI·콤마/세미콜론 이스케이프) ③ profile.tagline/taglineEn 한 줄 소개 ④ 밝은 프리셋 페이지/카드 토큰 `:root:not(.dark)` 스코프로 다크모드 케스케이드 수정.
  - 검증: typecheck 0 · integrity+deps 79/79 통과. 커밋 미실행(지시 대기).
- **인사이트:** ① 서브에이전트 maxTurns(30)는 4건 복합 위임에 부족 — 33 tool uses 시점 중단으로 share UI·vCard가 미완, 메인 세션이 마커 grep(`namecardShareButton`/`X-SOCIALPROFILE`/`:root:not(.dark)`)으로 완료도를 판정 후 잔여분 직접 마감. 복합 작업은 2건 이하로 쪼개 위임할 것. ② 번들 내장형 템플릿(namecard·link-card)은 개별 template 파일이 없어 에이전트 문서에 번들 구간을 명시하지 않으면 디자인 작업 위임이 공회전한다 — invitation처럼 분리 파일 구조가 에이전트 친화적.
