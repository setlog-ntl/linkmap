# Linkmap 대시보드 전면 개선 기획

> 작성일: 2026-02-18
> 상태: 컨셉 확정 (결정사항 반영 완료)
> 최종 정리: 2026-02-18

---

## Quick Start (구현 세션용 요약)

> 이 섹션은 새 Claude 세션이 이 문서만 읽고 바로 구현에 착수할 수 있도록 핵심만 요약합니다.

### 무엇을 만드는가
`/project/[id]` 개요 페이지를 **3-Column Layer Architecture Dashboard**로 전면 교체한다.
- 좌측: **Frontend** (배포, 도메인, 분석, 인증 영역)
- 중앙: **내 프로젝트** (메트릭 허브)
- 우측: **Backend** (DB, 결제, 이메일, 스토리지, 서버)
- 하단: **개발환경** (IDE, AI, CI/CD, 모니터링) — 전체 가로 확장
- 서비스 간 **연결선**(SVG 오버레이)으로 초록/빨강/노랑 상태 시각화

### 4-Phase 구현 순서

| Phase | 핵심 | Migration | 주요 산출물 |
|-------|------|-----------|------------|
| **1. MVP** | 3-Column 레이아웃 + 컴팩트 카드 | `027_dashboard_layer.sql` | 대시보드 API, 레이아웃, 모바일 탭 |
| **2. 연결** | SVG 연결선 + 확장 카드 + 자동/수동 연결 | `028_connection_types.sql` | 연결선 렌더링, connections 페이지 |
| **3. 커스텀** | 레이어 오버라이드 + 온보딩 + i18n | `029_service_layer_overrides.sql` | 오버라이드 UI, 빈 상태, 퀵액션 |
| **4. 고도화** | 성능 최적화 + 프리셋 + 신규 서비스 27개 | - | 인증 영역 UI, 접근성 |

### 생성/수정해야 할 핵심 파일

**신규 생성:**
| 파일 | Phase | 역할 |
|------|:-----:|------|
| `supabase/migrations/027_dashboard_layer.sql` | 1 | services 테이블에 레이어 컬럼 추가 |
| `supabase/migrations/028_connection_types.sql` | 2 | user_connections 확장 |
| `supabase/migrations/029_service_layer_overrides.sql` | 3 | 오버라이드 테이블 생성 |
| `src/app/api/projects/[id]/dashboard/route.ts` | 1 | 대시보드 통합 API (6개 소스 JOIN) |
| `src/app/project/[id]/connections/page.tsx` | 2 | 연결 관리 페이지 (신규) |
| `src/components/dashboard/` | 1 | 3-Column 레이아웃, 카드 컴포넌트 |
| `src/lib/queries/dashboard.ts` | 1 | TanStack Query 훅 (useDashboard 등) |
| `src/lib/queries/connections.ts` | 2 | 연결 CRUD 훅 |
| `src/stores/dashboard-store.ts` | 1 | Zustand 스토어 (탭, 확장 카드 등) |
| `src/lib/i18n/dashboard.ts` | 3 | 대시보드 라벨 한/영 |

**수정 필요:**
| 파일 | Phase | 변경 내용 |
|------|:-----:|----------|
| `src/app/project/[id]/page.tsx` | 1 | 기존 Overview → 3-Column 대시보드로 교체 |
| `src/components/project/project-tabs.tsx` | 1 | 7탭 → 3그룹(메인/서브/관리) 구조로 변경 |
| `src/data/services.ts` | 1 | `dashboard_layer`, `dashboard_subcategory` 필드 추가 |
| `src/app/project/[id]/services/page.tsx` | 1 | 역할 변경: 서비스 카탈로그 + 추가 |

### 핵심 기술 스택 참조
- **레이아웃**: CSS Grid (`grid-cols-[1fr_auto_1fr]`) + 하단 `col-span-full`
- **카드 애니메이션**: `framer-motion` AnimatePresence (확장/축소)
- **연결선**: SVG `<path>` 오버레이 (React Flow와 별도, 경량)
- **데이터**: TanStack Query (staleTime 30초, 백그라운드 60초) + Zustand UI 상태
- **반응형**: 데스크톱 3단 / 태블릿 3단 축소 / 모바일(<768px) 탭 전환

### 상세 스펙 위치 가이드

| 주제 | 이 문서 내 위치 |
|------|---------------|
| 전체 대시보드 레이아웃 다이어그램 | 섹션 2.1 |
| 서비스 카드 와이어프레임 7종 | 섹션 2.4 |
| 빈 상태 & 온보딩 플로우 | 섹션 2.6 |
| 연결선 렌더링 (SVG, 자동/수동, 방향) | 섹션 2.7 |
| 에러/로딩 상태 | 섹션 2.8 |
| 54개 서비스 전체 분류표 | 섹션 3.2 |
| 다크모드 색상 체계 | 섹션 3.3 |
| i18n 라벨 매핑 | 섹션 3.6 |
| 라우팅 AS-IS → TO-BE | 섹션 4 |
| SQL 마이그레이션 + ER 다이어그램 | 섹션 5.2~5.6 |
| API 응답 TypeScript 인터페이스 | 섹션 5.7 |
| TanStack Query 훅 + 캐싱 전략 | 섹션 5.8 |
| Zustand 스토어 인터페이스 | 섹션 5.8 |
| 서비스 맵 ↔ 대시보드 동기화 | 섹션 5.9 |
| 레이어 오버라이드 UI | 섹션 5.10 |
| 4-Phase 구현 계획 상세 | 섹션 7 |

---

## 1. 현행 대시보드 구조 (AS-IS)

### 1.1 페이지 흐름
```
랜딩(/) → 대시보드(/dashboard) → 프로젝트(/project/[id])
```

### 1.2 대시보드 페이지 (/dashboard)
- 프로젝트 목록을 그리드 형태로 표시 (ProjectCard)
- 프로젝트 생성 다이얼로그 / 템플릿 기반 생성
- 프로젝트가 없을 때 빈 상태(Empty State) 표시
- i18n (한/영) 지원

### 1.3 프로젝트 상세 (/project/[id])
- **탭 네비게이션** (ProjectTabs):
  - 개요 | 서비스 맵 | 서비스 목록 | 환경변수 | 상태 모니터링 | 감사 로그 | 설정
- **개요 페이지 (Overview)**:
  - 메트릭 카드 3개: 연결 서비스 수, 환경변수 수, 셋업 진행률
  - 서비스 헬스 상태 (healthy/degraded/unhealthy/unknown)
  - 스마트 제안 (미연결 서비스 추천)
  - 빠른 액션 버튼 (서비스 맵, 서비스 목록, 환경변수)
  - 연결된 서비스 리스트

### 1.4 현행 구조의 특징
- 프로젝트 단위로 모든 기능이 구성됨
- 서비스 연결 → 환경변수 관리 → 상태 모니터링의 선형 흐름
- React Flow 기반 서비스 맵 시각화
- AES-256-GCM 환경변수 암호화

---

## 2. 개선 방향 (TO-BE)

### 2.1 핵심 컨셉: 3-Column Layer Architecture Dashboard

프로젝트의 기술 스택을 **가로 3단 레이어**로 분리하여, 개발자의 멘탈 모델에 맞는 직관적인 대시보드를 제공한다.

```
┌──────────────────────────────────────────────────────────────────┐
│                         프로젝트 헤더                              │
├───────────────────┬───────────────────┬──────────────────────────┤
│   FRONTEND        │   내 프로젝트      │   BACKEND               │
│                   │                   │                          │
│  ┌─────────────┐  │  ┌─────────────┐  │  ┌─────────────┐        │
│  │ 배포 서비스  │  │  │  내 서비스   │  │  │ DB 서비스   │        │
│  │ (Vercel)    │──│──│  (메인)     │──│──│ (Supabase)  │        │
│  └─────────────┘  │  └─────────────┘  │  └─────────────┘        │
│  ┌─────────────┐  │                   │  ┌─────────────┐        │
│  │ 도메인      │  │                   │  │ 결제         │        │
│  │ (Cloudflare)│  │                   │  │ (Stripe)    │        │
│  └─────────────┘  │                   │  └─────────────┘        │
│  ┌─────────────┐  │                   │  ┌─────────────┐        │
│  │ Analytics   │  │                   │  │ 스토리지     │        │
│  │ (GA4)       │  │                   │  │ (S3/R2)     │        │
│  └─────────────┘  │                   │  └─────────────┘        │
│                   │                   │  ┌─────────────┐        │
│  ┌ ─ ─ ─ ─ ─ ─┐  │                   │  │ 이메일      │        │
│  ┊  인증 영역   ┊  │                   │  │ (Resend)    │        │
│  ┊┌───────────┐┊  │                   │  └─────────────┘        │
│  ┊│ Clerk     │┊  │                   │  ┌─────────────┐        │
│  ┊└───────────┘┊  │                   │  │ 서버/인프라  │        │
│  ┊┌───────────┐┊  │                   │  │ (Railway)   │        │
│  ┊│ Kakao     │┊  │                   │  └─────────────┘        │
│  ┊└───────────┘┊  │                   │                          │
│  ┊┌───────────┐┊  │                   │                          │
│  ┊│ Google    │┊  │                   │                          │
│  ┊└───────────┘┊  │                   │                          │
│  └ ─ ─ ─ ─ ─ ─┘  │                   │                          │
├───────────────────┴───────────────────┴──────────────────────────┤
│   개발환경 (전체 가로 확장)                                        │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐            │
│  │ VS Code  │ │ Claude   │ │ GPT      │ │ Cursor   │            │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘            │
│  ┌──────────┐ ┌──────────┐                                      │
│  │ GH Actions│ │ Sentry  │       [+ 서비스 추가]                  │
│  └──────────┘ └──────────┘                                      │
└──────────────────────────────────────────────────────────────────┘

연결선 색상:
  ──── 초록: 연결됨    ──── 빨강: 끊김/오류    ──── 노랑: 설정 중

* 인증 영역(점선 박스): Frontend 내 별도 구분된 섹션으로,
  인증 플랫폼(Clerk, NextAuth)과 소셜 로그인(Kakao, Google 등)을 함께 표시.
  실제 인증 처리는 백엔드에서 이루어지나, 사용자 접점이므로 Frontend에 배치.
```

### 2.2 레이어 정의

| 레이어 | 위치 | 포함 서비스 | 기준 |
|--------|------|-------------|------|
| **Frontend** | 좌측 | 배포(Vercel, Netlify), 도메인(Cloudflare), Analytics(GA4, PostHog), **인증 영역**(Clerk, NextAuth, 소셜 로그인) | "사용자에게 보이는 외부 노출 서비스" |
| **내 프로젝트** | 중앙 | 프로젝트 메인 정보, 메트릭 요약 | "내가 만드는 서비스 그 자체" |
| **Backend** | 우측 | DB(Supabase, Firebase), 결제(Stripe), 스토리지(S3, Cloudinary), 이메일(Resend), 서버(Railway) | "서버에서 동작하는 서비스" |
| **개발환경** | 하단 전체 | IDE(VS Code, Cursor), AI(Claude, GPT, Gemini), CI/CD(GitHub Actions), 모니터링(Sentry) | "모든 레이어에 걸치는 도구" |

### 2.3 연결 상태 시각화

각 서비스 카드 사이에 **연결선**을 표시하며, 상태에 따라 색상이 달라진다:

| 상태 | 색상 | 의미 |
|------|------|------|
| 연결됨 | 초록 | API 키/설정 완료, 헬스체크 통과 |
| 끊김/오류 | 빨강 | 연결 실패, 인증 만료 |
| 미확인/설정 중 | 노랑 | 서비스 추가됨, 아직 설정 미완료 |

### 2.4 서비스 카드 UI 와이어프레임

#### 2.4.1 카드 유형 (3종)

대시보드에서 사용하는 카드는 **컴팩트(기본)**, **확장(상세)**, **내 프로젝트(중앙)** 3종이다.

---

#### 2.4.2 컴팩트 카드 (기본 상태)

3-Column 레이어 안에서 서비스를 표시하는 **기본 카드**. 최소 정보만 노출.

```
┌─────────────────────────────────────┐
│ ● [아이콘] 서비스명          ⋮  ↗   │  ← 헤더 (1줄)
│─────────────────────────────────────│
│ 🔗 proj_abc123...                   │  ← 연결 ID (마스킹)
│ 🏷  env: 3/5 완료                    │  ← 환경변수 진행률
└─────────────────────────────────────┘
  40~48px 높이 · 카테고리별 좌측 액센트 바
```

**구성 요소:**
| 영역 | 요소 | 설명 |
|------|------|------|
| 좌측 액센트 | 3px 세로 바 | 카테고리 색상 (getCategoryStyle 재활용) |
| 상태 표시등 | ● 원형 dot | 초록/빨강/노랑 + 연결됨일 때 ping 애니메이션 |
| 아이콘 | ServiceIcon (20px) | 기존 서비스 아이콘 재활용 |
| 서비스명 | text-sm, font-medium | 1줄, truncate |
| ⋮ 메뉴 | DropdownMenu | hover 시 표시: 설정, 문서, 연결 해제 |
| ↗ 외부링크 | 아이콘 버튼 (12px) | 서비스 대시보드 URL 바로가기 |
| 연결 ID | text-xs, muted | 계정ID/프로젝트ID 앞 12자 + "..." 마스킹 |
| 환경변수 | text-xs + 미니 진행바 | 설정된 수 / 필요한 수 (3/5 형식) |

**상태별 스타일 변화:**
| 상태 | 액센트 바 | 배경 | 상태 dot | 보더 |
|------|----------|------|---------|------|
| 연결됨 | 카테고리색 | 기본 | 초록 + ping | 없음 |
| 설정 중 | 카테고리색 50% | 기본 | 노랑 | 점선 (dashed) |
| 끊김/오류 | 빨강 | red/5 | 빨강 + ping | 빨강 실선 |

---

#### 2.4.3 확장 카드 (클릭 시 펼침)

컴팩트 카드를 클릭하면 **아래로 확장**되어 상세 정보를 표시.
framer-motion `AnimatePresence`로 height 애니메이션 적용.

```
┌─────────────────────────────────────┐
│ ● [아이콘] Vercel               ⋮ ↗ │  ← 컴팩트 (동일)
│─────────────────────────────────────│
│ 🔗 proj_abc123...                   │
│ 🏷  env: 3/5 완료                    │
├─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┤  ← 확장 영역 (접기/펼치기)
│                                     │
│  연결 정보                           │
│  ┌───────────────────────────────┐  │
│  │ Account: user@email.com      │  │
│  │ Project: my-app-prod         │  │
│  │ Domain:  myapp.vercel.app    │  │
│  └───────────────────────────────┘  │
│                                     │
│  환경변수          [전체보기 →]       │
│  ┌───────────────────────────────┐  │
│  │ ✅ VERCEL_TOKEN       ••••   │  │
│  │ ✅ VERCEL_ORG_ID      ••••   │  │
│  │ ⚠️ VERCEL_PROJECT_ID  미설정  │  │
│  └───────────────────────────────┘  │
│                                     │
│  헬스체크   ● Healthy  (120ms)       │
│  최근 확인: 2분 전                    │
│                                     │
│  ┌────────┐ ┌────────┐ ┌────────┐  │
│  │ 📋 복사 │ │ 📄 문서 │ │ ⚙ 설정 │  │  ← 퀵 액션 버튼
│  └────────┘ └────────┘ └────────┘  │
└─────────────────────────────────────┘
```

**확장 영역 구성:**
| 섹션 | 내용 | 비고 |
|------|------|------|
| 연결 정보 | 계정 이메일, 프로젝트명, 도메인 등 | 서비스별 동적 필드 |
| 환경변수 | 변수명 + 설정 상태 (✅/⚠️) + 값 마스킹 | 클릭 시 복사, [전체보기]로 env 페이지 이동 |
| 헬스체크 | 상태 dot + 텍스트 + 응답시간 + 마지막 확인 | 기존 HealthCheck 타입 재활용 |
| 퀵 액션 | 복사(API키), 문서(외부), 설정(내부) | 3개 고정 버튼 |

---

#### 2.4.4 내 프로젝트 카드 (중앙 컬럼)

3-Column의 가운데에 위치하는 **허브 카드**. 일반 서비스 카드와 다른 형태.

```
┌─────────────────────────────────────┐
│         [프로젝트 아이콘]             │
│          내 프로젝트명                │
│      "프로젝트 설명 한 줄..."         │
├─────────────────────────────────────┤
│                                     │
│  서비스 현황                          │
│  ┌─────┐  ┌─────┐  ┌─────┐         │
│  │ 12  │  │  28 │  │ 85% │         │
│  │서비스│  │ ENV │  │진행률│         │
│  └─────┘  └─────┘  └─────┘         │
│                                     │
│  연결 상태 요약                       │
│  ●●●●●●●○○○  7/10 연결됨            │
│                                     │
│  ┌─────────────────────────────┐    │
│  │   [+ 서비스 추가]            │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │   [📊 서비스 맵 보기]        │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

**구성 요소:**
| 영역 | 내용 | 비고 |
|------|------|------|
| 프로젝트 정보 | 아이콘 + 이름 + 설명 | 프로젝트 메타 정보 |
| 메트릭 3종 | 연결 서비스 수, 환경변수 수, 진행률 % | 기존 KPI 카드 축소 버전 |
| 연결 상태 바 | 채워진 원(●) 도트 시퀀스 | 초록=연결, 빨강=오류, 빈원=미설정 |
| CTA 버튼 | 서비스 추가, 서비스 맵 보기 | 주요 액션 2개 |

---

#### 2.4.5 서비스 추가 카드 (+ 카드)

각 레이어 하단에 표시되는 서비스 추가 유도 카드.

```
┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
┊                                     ┊
┊          [+]  서비스 추가             ┊  ← 점선 보더, muted 텍스트
┊                                     ┊
└ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
```
- 점선(dashed) 보더 + muted-foreground 텍스트
- hover 시 보더 primary 색상 전환
- 클릭 → 서비스 카탈로그 다이얼로그 (해당 레이어 필터 적용)

---

#### 2.4.6 인증 영역 카드 그룹 (Frontend 내)

Frontend 컬럼 내에서 인증 서비스를 **점선 박스로 묶어** 시각적으로 구분.

```
┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
┊  🔐 인증                   [+ 추가] ┊  ← 그룹 헤더 (점선 내부)
┊                                     ┊
┊  ┌─────────────────────────────┐    ┊
┊  │ ● [🔑] Clerk          ⋮ ↗ │    ┊  ← 인증 플랫폼
┊  │ 🔗 org_abc...  env: 2/2    │    ┊
┊  └─────────────────────────────┘    ┊
┊  ┌─────────────────────────────┐    ┊
┊  │ ● [K] Kakao Login     ⋮ ↗ │    ┊  ← 소셜 로그인
┊  │ 🔗 app_id...  env: 2/2    │    ┊
┊  └─────────────────────────────┘    ┊
┊  ┌─────────────────────────────┐    ┊
┊  │ ● [G] Google OAuth    ⋮ ↗ │    ┊
┊  │ 🔗 client...  env: 2/3    │    ┊
┊  └─────────────────────────────┘    ┊
└ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
```

**인증 영역 특징:**
- 점선 보더로 일반 Frontend 서비스와 시각적 구분
- 그룹 헤더에 🔐 아이콘 + "인증" 라벨 + [+ 추가] 버튼
- 내부 카드는 일반 컴팩트 카드와 동일 규격
- 접기/펼치기 가능 (서비스가 많을 때)

---

#### 2.4.7 개발환경 카드 (하단 레이어)

하단 전체 확장 레이어에서는 **가로 배치** 컴팩트 카드를 사용.

```
┌──────────────────────────────────────────────────────────────────┐
│ 🛠 개발환경                                          [+ 추가]    │
│                                                                  │
│ IDE                        AI 코딩                               │
│ ┌──────────┐ ┌──────────┐  ┌──────────┐ ┌──────────┐            │
│ │● VS Code │ │● Cursor  │  │● Claude  │ │● GPT     │            │
│ │  v1.89   │ │  v0.45   │  │  Code    │ │  API     │            │
│ └──────────┘ └──────────┘  └──────────┘ └──────────┘            │
│                                                                  │
│ CI/CD                      모니터링                               │
│ ┌──────────┐ ┌──────────┐  ┌──────────┐                         │
│ │● GitHub  │ │○ Docker  │  │● Sentry  │                         │
│ │  Actions │ │  미설정   │  │  DSN 설정│                         │
│ └──────────┘ └──────────┘  └──────────┘                         │
└──────────────────────────────────────────────────────────────────┘
```

**개발환경 레이어 특징:**
- 서브카테고리(IDE, AI, CI/CD, 모니터링)별 **라벨 그룹핑**
- 카드 크기: 일반 컴팩트 카드보다 작은 **미니 카드** (아이콘+이름+1줄 정보)
- 가로 배치 (flex-wrap) — 화면 너비에 따라 자동 줄바꿈
- 상태 dot은 동일 (초록/빨강/노랑)

---

#### 2.4.8 모바일 탭 뷰 카드

모바일(<768px)에서는 탭 전환 방식이므로 카드가 **세로 풀 너비**로 표시.

```
┌─────────────────────────────────────┐
│  [Frontend] [내 프로젝트] [Backend] [개발환경] │  ← 탭 바
├─────────────────────────────────────┤
│                                     │
│  배포/호스팅                         │
│  ┌─────────────────────────────┐    │
│  │ ● [▲] Vercel          ⋮ ↗  │    │  ← 풀 너비 컴팩트 카드
│  │ 🔗 proj_abc... env: 3/3    │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │ ● [N] Netlify         ⋮ ↗  │    │
│  │ 🔗 site_xyz... env: 2/2    │    │
│  └─────────────────────────────┘    │
│                                     │
│  분석/추적                           │
│  ┌─────────────────────────────┐    │
│  │ ● [📊] PostHog        ⋮ ↗  │    │
│  │ 🔗 key_abc...  env: 2/2    │    │
│  └─────────────────────────────┘    │
│                                     │
│  ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐   │
│  ┊  🔐 인증              [+]   ┊   │  ← 인증 영역 (점선)
│  ┊  ┌─────────────────────┐    ┊   │
│  ┊  │ ● Clerk     env:2/2│    ┊   │
│  ┊  └─────────────────────┘    ┊   │
│  ┊  ┌─────────────────────┐    ┊   │
│  ┊  │ ● Kakao     env:2/2│    ┊   │
│  ┊  └─────────────────────┘    ┊   │
│  └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘   │
│                                     │
│  ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐   │
│  ┊     [+] 서비스 추가          ┊   │
│  └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘   │
└─────────────────────────────────────┘
```

---

#### 2.4.9 카드 인터랙션 정리

| 동작 | 컴팩트 카드 | 확장 카드 | 내 프로젝트 카드 |
|------|:----------:|:--------:|:--------------:|
| 클릭 | 확장 토글 | 축소 토글 | 프로젝트 설정 |
| hover | 그림자 + scale(1.02) | - | 그림자 |
| ⋮ 메뉴 | 설정/문서/연결해제 | - | - |
| ↗ 아이콘 | 외부 대시보드 새탭 | - | - |
| 복사 버튼 | - | API 키 복사 | - |
| + 추가 | - | - | 서비스 카탈로그 열기 |
| 서비스 맵 | - | - | React Flow 뷰 전환 |

#### 2.4.10 기존 컴포넌트 재활용 계획

| 기존 컴포넌트 | 재활용 요소 | 신규 카드 적용 |
|-------------|-----------|--------------|
| `ServiceIcon` | 아이콘 렌더링 | 모든 카드 |
| `getCategoryStyle()` | 카테고리 색상/그라데이션 | 액센트 바, 보더 |
| `Badge` (shadcn) | 상태/태그 표시 | 환경변수 진행률, 헬스 상태 |
| `DropdownMenu` (shadcn) | ⋮ 메뉴 | 컴팩트 카드 |
| `AnimatePresence` (framer) | 확장 애니메이션 | 확장 카드 |
| `ServiceBentoCard` 패턴 | 접기/펼치기 구조 | 확장 카드 기반 |

### 2.5 핵심 인터랙션

1. **서비스 추가**: 각 레이어의 [+ 서비스 추가] 카드 클릭 → 카탈로그 다이얼로그 (해당 레이어 필터) → 선택 → 컴팩트 카드 자동 생성
2. **연결 상세 확인**: 컴팩트 카드 클릭 → 확장 카드로 펼쳐서 연결 정보 + 환경변수 + 헬스체크 확인
3. **전체 현황 파악**: 3-Column + 개발환경 레이어에서 한눈에 모든 연결 상태(초록/빨강/노랑) 확인
4. **퀵 액션**: 확장 카드 내 버튼으로 API 키 복사, 문서 열기, 설정 페이지 이동
5. **외부 이동**: ↗ 아이콘으로 서비스 대시보드 바로가기 (새 탭)

### 2.6 빈 상태 & 온보딩 플로우

#### 2.6.1 신규 프로젝트 빈 상태

서비스가 0개인 프로젝트의 3-Column 대시보드 표시.

```
┌───────────────────┬───────────────────┬──────────────────────────┐
│   FRONTEND        │   내 프로젝트      │   BACKEND               │
│                   │                   │                          │
│  ┌ ─ ─ ─ ─ ─ ─┐  │  ┌─────────────┐  │  ┌ ─ ─ ─ ─ ─ ─┐        │
│  ┊             ┊  │  │  프로젝트명  │  │  ┊             ┊        │
│  ┊  배포 서비스  ┊  │  │  0 서비스    │  │  ┊  DB 서비스    ┊        │
│  ┊  를 추가하세요┊  │  │  0 ENV      │  │  ┊  를 추가하세요 ┊        │
│  ┊             ┊  │  │  0% 진행     │  │  ┊              ┊        │
│  ┊  [+ 추가]   ┊  │  │             │  │  ┊  [+ 추가]    ┊        │
│  └ ─ ─ ─ ─ ─ ─┘  │  └─────────────┘  │  └ ─ ─ ─ ─ ─ ─┘        │
├───────────────────┴───────────────────┴──────────────────────────┤
│   개발환경                                                        │
│   ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐  │
│   ┊  개발 도구를 추가하세요              [+ 추가]               ┊  │
│   └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘  │
└──────────────────────────────────────────────────────────────────┘
```

**빈 상태 요소:**
- 각 레이어에 **점선 플레이스홀더** + 안내 텍스트 + [+ 추가] 버튼
- 내 프로젝트 카드는 메트릭 0/0/0% 표시
- 연결선 없음

#### 2.6.2 온보딩 흐름

첫 진입 시 단계적 안내:

```
[1단계] 프로젝트 생성 완료
   ↓
[2단계] "서비스를 추가해보세요" 툴팁 표시
   ↓  (프리셋 추천: "Next.js + Supabase + Vercel 한 번에 추가")
[3단계] 첫 서비스 추가 → 카드가 해당 레이어에 나타남
   ↓
[4단계] "환경변수를 설정하세요" 카드 내 노랑 상태 강조
   ↓
[5단계] 환경변수 설정 완료 → 초록 상태 전환 + 연결선 활성화
```

**온보딩 UI:**
- 첫 진입: 중앙에 **"시작하기"** 오버레이 (프리셋 추천 3개)
- 진행 중: 완료하지 않은 항목에 **pulse 애니메이션** 강조
- 완료 후: 온보딩 상태 `localStorage`에 저장, 다시 표시 안 함

---

### 2.7 연결선 렌더링

#### 2.7.1 렌더링 방식

3-Column 대시보드의 연결선은 **SVG 오버레이**로 구현한다.
(서비스 맵의 React Flow와는 별도 — 경량 SVG 기반)

```
┌──────────────────────────────────────────────────────────┐
│  SVG 오버레이 레이어 (position: absolute, pointer-events)  │
│                                                          │
│  Vercel ───────────────────────── Supabase               │
│                    │                                     │
│  Cloudflare ───────┤                                     │
│                    │              Stripe                  │
│  GA4 ──────────────┘              │                      │
│                                   │                      │
│  Clerk ───────────────────────────┘                      │
└──────────────────────────────────────────────────────────┘
```

**기술 구현:**
| 요소 | 구현 방식 |
|------|----------|
| 연결선 | SVG `<path>` 또는 `<line>` — 카드 DOM 위치 기반 좌표 계산 |
| 색상 | `stroke` 속성으로 connection_status 색상 적용 |
| 점선 | pending 상태: `stroke-dasharray="5,5"` |
| 애니메이션 | error 상태: CSS `animate-pulse` on stroke |
| 리사이즈 | `ResizeObserver`로 카드 위치 변경 시 좌표 재계산 |
| 모바일 | 탭 뷰에서는 연결선 숨김 (레이어 간 이동 불가) |

#### 2.7.2 기본 자동 연결

서비스를 추가하면 **기본 연결이 자동 생성**된다.
`service_dependencies` 테이블의 기존 의존 관계 데이터를 활용.

```
자동 연결 규칙:
1. 서비스 A를 추가
2. service_dependencies에서 A가 의존하는 서비스 B 조회
3. B가 이미 프로젝트에 추가되어 있으면 → user_connections 자동 생성
4. connection_status = 'pending' (노랑)
5. 양쪽 서비스의 환경변수가 모두 설정되면 → 'active' (초록)으로 자동 전환
```

**자동 연결 예시:**
| 추가한 서비스 | 자동 연결 대상 | 연결 유형 | 조건 |
|-------------|-------------|----------|------|
| Vercel | Supabase | deploy_target | Supabase가 프로젝트에 존재 |
| Clerk | Supabase | auth_provider | Supabase가 프로젝트에 존재 |
| Sentry | Vercel | monitoring | Vercel이 프로젝트에 존재 |
| Stripe | Supabase | data_source | Supabase가 프로젝트에 존재 |

#### 2.7.3 사용자 수동 연결 설정

사용자가 직접 연결을 **추가/편집/삭제**할 수 있다.

**연결 추가 UI:**
```
┌─────────────────────────────────────┐
│  🔗 연결 추가                        │
│                                     │
│  출발 서비스:  [Vercel      ▼]      │
│  도착 서비스:  [Supabase    ▼]      │
│  연결 유형:    [deploy_target ▼]    │
│  설명 (선택):  [배포 시 DB 연결___]  │
│                                     │
│         [취소]    [연결 추가]         │
└─────────────────────────────────────┘
```

**수동 연결 인터랙션:**
| 동작 | 방법 | 설명 |
|------|------|------|
| **추가** | 내 프로젝트 카드의 [+ 연결 추가] 또는 연결 관리 탭 | 출발/도착 서비스 + 유형 선택 |
| **편집** | 연결선 클릭 → 팝오버 | 유형, 설명, 상태 변경 |
| **삭제** | 연결선 클릭 → 팝오버 → [삭제] | 자동 연결도 삭제 가능 |
| **상태 변경** | 연결선 클릭 → 팝오버 → 상태 토글 | active ↔ error ↔ pending |

**자동 vs 수동 구분:**
| 구분 | user_connections.metadata | 표시 |
|------|--------------------------|------|
| 자동 연결 | `{ "auto": true }` | 연결선에 "자동" 라벨 |
| 수동 연결 | `{ "auto": false }` | 연결선에 사용자 설명 라벨 |

#### 2.7.4 연결선 방향성

```
Frontend 컬럼              내 프로젝트           Backend 컬럼
┌──────────┐                  │               ┌──────────┐
│ Vercel   │─── deploy_target─┼──────────────→│ Supabase │
└──────────┘                  │               └──────────┘
┌──────────┐                  │               ┌──────────┐
│ Clerk    │── auth_provider──┼──────────────→│ Supabase │
└──────────┘                  │               └──────────┘
                              │
개발환경 레이어                 │
┌──────────┐                  │               ┌──────────┐
│ Sentry   │──── monitoring───┼──→ Vercel     │          │
│          │──── monitoring───┼──────────────→│ Supabase │
└──────────┘                  │               └──────────┘
```

**방향 규칙:**
- source → target 방향으로 화살표 표시
- 같은 컬럼 내 연결: 세로 곡선
- 다른 컬럼 간 연결: 내 프로젝트(중앙)를 경유하는 베지어 커브
- 개발환경 → 상위 컬럼: 아래에서 위로 곡선

---

### 2.8 에러 / 로딩 상태

#### 2.8.1 로딩 상태 (스켈레톤)

대시보드 데이터 로드 중 **3-Column 스켈레톤 UI** 표시.

```
┌───────────────────┬───────────────────┬──────────────────────────┐
│   ░░░░░░░░░░░░░   │   ░░░░░░░░░░░░   │   ░░░░░░░░░░░░░          │
│   ░░░░░░░░░░░░░   │   ░░░░░░░░░░░░   │   ░░░░░░░░░░░░░          │
│   ░░░░░░░░░░░░░   │   ░░░  ░░  ░░░   │   ░░░░░░░░░░░░░          │
│   ░░░░░░░░░░░░░   │                   │   ░░░░░░░░░░░░░          │
│                   │                   │   ░░░░░░░░░░░░░          │
├───────────────────┴───────────────────┴──────────────────────────┤
│   ░░░░░░░  ░░░░░░░  ░░░░░░░  ░░░░░░░                            │
└──────────────────────────────────────────────────────────────────┘
```

- shadcn `Skeleton` 컴포넌트 사용
- 컬럼 구조는 유지한 채 카드 영역만 스켈레톤
- 헤더/탭은 즉시 렌더링 (서버 컴포넌트)

#### 2.8.2 에러 상태

| 에러 유형 | 표시 방식 | 복구 방법 |
|----------|----------|----------|
| 대시보드 API 전체 실패 | 에러 카드 + "다시 시도" 버튼 | TanStack Query `retry` + 수동 |
| 개별 서비스 데이터 실패 | 해당 카드만 에러 상태 (빨강 보더) | 카드 내 "다시 시도" |
| 헬스체크 실패 | 카드 상태 dot 빨강 + 마지막 성공 시간 | 수동 헬스체크 실행 |
| 연결선 데이터 실패 | 연결선 숨김 + 상단 경고 배너 | API 재호출 |
| 네트워크 끊김 | 전체 오버레이 + "오프라인" 표시 | 자동 재연결 감지 |

---

## 3. 확정된 결정사항

### 3.1 네이밍 — 확정
- 가운데 컬럼: **"내 프로젝트"**
- 하단 레이어: **"개발환경"** (IDE & AI + CI/CD + 모니터링)

### 3.2 서비스 분류 — 전체 상세 매핑

각 서비스에 **기본 레이어 + 서브카테고리**를 사전 지정하여 자동 배치한다.

**분류 기준:**
- **Frontend**: "사용자 브라우저/외부에 노출되거나, 서비스를 외부로 내보내는 역할"
- **Backend**: "서버에서 동작하며, 데이터 처리/저장/인증/통신을 담당"
- **개발환경**: "모든 레이어에 걸쳐 개발 과정 전체를 지원하는 도구"

---

#### Frontend 레이어 (좌측)

| 서브카테고리 | 서비스 | 기존코드 | 분류 근거 |
|-------------|--------|:--------:|-----------|
| **배포/호스팅** | Vercel | O | 프론트엔드 빌드 결과를 사용자에게 전달 |
| | Netlify | O | 정적 사이트/JAMstack 배포 |
| | GitHub Pages | - | 신규: 정적 배포 |
| **도메인/DNS** | Cloudflare | - | 신규: 도메인 관리 + CDN + 보안 |
| | Route53 | - | 신규: AWS DNS 서비스 |
| | Namecheap | - | 신규: 도메인 등록 |
| **분석/추적** | PostHog | O | 프로덕트 분석 (사용자 행동 추적) |
| | Google Analytics | - | 신규: 웹 트래픽 분석 |
| | Hotjar | - | 신규: 히트맵/세션 레코딩 |
| | Mixpanel | - | 신규: 이벤트 기반 분석 |
| | | | |
| **인증 영역** *(점선 구분 — 별도 시각 섹션)* | | | |
| 인증 플랫폼 | Clerk | O | 인증 SaaS (사용자가 로그인 UI로 접하는 서비스) |
| | NextAuth / Auth.js | O | 인증 라이브러리 (로그인 화면 구현) |
| 소셜 로그인 | Kakao Login | O | 로그인 버튼 = 사용자 접점 |
| | Google OAuth | O | 로그인 버튼 = 사용자 접점 |
| | Naver Login | O | 로그인 버튼 = 사용자 접점 |
| | Apple Login | O | 로그인 버튼 = 사용자 접점 |
| | GitHub OAuth | O | 로그인 버튼 = 사용자 접점 |

> **인증 영역 배치 근거**: 인증은 실제 처리가 백엔드에서 이루어지지만,
> 사용자가 직접 상호작용하는 접점(로그인 화면, 소셜 버튼)이므로 **Frontend에 배치**.
> 대시보드에서는 **점선 박스**로 일반 Frontend 서비스와 시각적으로 구분한다.
> Supabase Auth, Firebase Auth는 해당 부모 서비스(Backend)의 하위 기능으로 별도 표시하지 않는다.

**Frontend 소계: 19개 (기존 12 + 신규 7)**

---

#### Backend 레이어 (우측)

| 서브카테고리 | 서비스 | 기존코드 | 분류 근거 |
|-------------|--------|:--------:|-----------|
| **데이터베이스** | Supabase | O | PostgreSQL + Auth + Storage 통합 |
| | Firebase | O | NoSQL + 실시간 DB |
| | PlanetScale | O | 서버리스 MySQL |
| | Neon | O | 서버리스 PostgreSQL |
| | Turso | - | 신규: 엣지 SQLite (libSQL) |
| | MongoDB Atlas | - | 신규: 클라우드 NoSQL |
| **결제** | Stripe | O | 결제 처리 API |
| | Lemon Squeezy | O | SaaS 결제 (MoR) |
| | Toss Payments | - | 신규: 국내 결제 |
| | PortOne (아임포트) | - | 신규: 국내 PG 통합 |
| **이메일/메시징** | Resend | O | 트랜잭셔널 이메일 API |
| | SendGrid | O | 대량 이메일 API |
| | Twilio | - | 신규: SMS/알림 |
| **스토리지** | Cloudinary | O | 이미지/비디오 CDN 변환 |
| | UploadThing | O | 파일 업로드 서비스 |
| | AWS S3 | O | 오브젝트 스토리지 |
| | Cloudflare R2 | - | 신규: S3 호환 스토리지 (이그레스 무료) |
| **서버/인프라** | Railway | O | PaaS 서버 배포 |
| | Fly.io | - | 신규: 엣지 컨테이너 배포 |
| | AWS Lambda | - | 신규: 서버리스 함수 |

**Backend 소계: 20개 (기존 9 + 신규 11)**

---

#### 개발환경 레이어 (하단 전체)

| 서브카테고리 | 서비스 | 기존코드 | 분류 근거 |
|-------------|--------|:--------:|-----------|
| **IDE** | VS Code | - | 신규: 코드 에디터 |
| | Cursor | - | 신규: AI 코드 에디터 |
| | WebStorm | - | 신규: JetBrains IDE |
| | Windsurf | - | 신규: AI 코드 에디터 |
| **AI 코딩** | Claude Code | O | AI 코드 어시스턴트 (Anthropic) |
| | OpenAI (GPT) | O | LLM API |
| | Anthropic API | O | LLM API |
| | Google Gemini | O | 멀티모달 AI API |
| | GitHub Copilot | - | 신규: AI 코드 자동완성 |
| **CI/CD** | GitHub (Actions) | O | 버전 관리 + CI/CD 파이프라인 |
| | GitLab CI | - | 신규: GitLab 파이프라인 |
| | Docker | - | 신규: 컨테이너 빌드/배포 |
| **모니터링** | Sentry | O | 에러 추적 (프론트+백엔드) |
| | Datadog | - | 신규: 인프라 모니터링 |
| | LogRocket | - | 신규: 프론트엔드 세션 리플레이 |

**개발환경 소계: 15개 (기존 6 + 신규 9)**

---

#### 전체 요약

| 레이어 | 기존 서비스 | 신규 추가 | 합계 |
|--------|:---------:|:--------:|:----:|
| Frontend (인증 영역 포함) | 12 | 7 | **19** |
| Backend | 9 | 11 | **20** |
| 개발환경 | 6 | 9 | **15** |
| **합계** | **27** | **27** | **54** |

### 3.3 연결 상태 — 3색 체계 확정 (라이트/다크)

| 상태 | 라이트 모드 | 다크 모드 | Tailwind 클래스 | 조건 |
|------|-----------|----------|----------------|------|
| 연결됨 | #22c55e | #4ade80 | `text-green-500` / `dark:text-green-400` | API 키/설정 완료 + 헬스체크 통과 |
| 끊김/오류 | #ef4444 | #f87171 | `text-red-500` / `dark:text-red-400` | 연결 실패, 인증 만료, 헬스체크 실패 |
| 설정 중 | #eab308 | #facc15 | `text-yellow-500` / `dark:text-yellow-400` | 서비스 추가됨, 설정 미완료 또는 미확인 |

> 다크모드에서는 밝기를 한 단계 올려(400) 어두운 배경 위에서 가독성을 확보한다.
> 연결선 SVG stroke도 동일 색상 적용 (`stroke` CSS 변수 활용).

### 3.4 반응형 전략 — 확정
| 화면 | 레이아웃 |
|------|----------|
| **데스크톱** (1024px+) | 가로 3단 + 하단 개발환경 |
| **태블릿** (768~1023px) | 가로 3단 축소 + 하단 개발환경 |
| **모바일** (<768px) | **탭 전환** (Frontend / 내 프로젝트 / Backend / 개발환경) |

### 3.5 서비스 카드 — 확정
각 서비스 카드에 표시되는 정보:
- 서비스 아이콘 + 이름
- 연결 상태 표시등 (초록/빨강/노랑)
- 연결된 계정 ID 또는 프로젝트 ID (간략 표시)
- 관련 페이지/대시보드 URL (외부 링크)
- 퀵 액션 (설정, 문서, API 키 복사)

### 3.6 i18n (다국어) — 대시보드 라벨

기존 `locale-store` + `t()` 함수 구조를 확장하여 대시보드 라벨을 처리한다.

| 키 | 한국어 (기본) | English |
|----|-------------|---------|
| `dashboard.layer.frontend` | Frontend | Frontend |
| `dashboard.layer.myProject` | 내 프로젝트 | My Project |
| `dashboard.layer.backend` | Backend | Backend |
| `dashboard.layer.devtools` | 개발환경 | Dev Tools |
| `dashboard.auth.title` | 인증 | Authentication |
| `dashboard.subcat.deploy` | 배포/호스팅 | Deploy / Hosting |
| `dashboard.subcat.domain` | 도메인/DNS | Domain / DNS |
| `dashboard.subcat.analytics` | 분석/추적 | Analytics |
| `dashboard.subcat.database` | 데이터베이스 | Database |
| `dashboard.subcat.payment` | 결제 | Payment |
| `dashboard.subcat.email` | 이메일/메시징 | Email / Messaging |
| `dashboard.subcat.storage` | 스토리지 | Storage |
| `dashboard.subcat.server` | 서버/인프라 | Server / Infra |
| `dashboard.subcat.ide` | IDE | IDE |
| `dashboard.subcat.ai` | AI 코딩 | AI Coding |
| `dashboard.subcat.cicd` | CI/CD | CI/CD |
| `dashboard.subcat.monitoring` | 모니터링 | Monitoring |
| `dashboard.action.addService` | 서비스 추가 | Add Service |
| `dashboard.action.addConnection` | 연결 추가 | Add Connection |
| `dashboard.status.connected` | 연결됨 | Connected |
| `dashboard.status.error` | 오류 | Error |
| `dashboard.status.pending` | 설정 중 | Pending |
| `dashboard.empty.title` | 시작하기 | Get Started |
| `dashboard.empty.description` | 서비스를 추가해 프로젝트를 구성하세요 | Add services to set up your project |

> `src/lib/i18n/` 하위에 `dashboard.ts` 파일을 추가하여 관리.

### 3.7 확장 기능 — 적용 확정
1. **서비스 프리셋/템플릿**: "Next.js + Supabase + Vercel" 같은 조합을 원클릭으로 추가
2. **서비스 간 의존 관계 그래프**: 어떤 Frontend 서비스가 어떤 Backend 서비스를 사용하는지 시각화
3. **서비스별 퀵 액션**: 문서 열기, API 키 복사, 상태 확인, 외부 대시보드 이동
4. **서비스 추가 버튼**: 대시보드에서 직접 [+ 서비스 추가] → 카탈로그 → 기본 레이어에 자동 배치
5. **영역별 접기/펼치기**: 서비스 개수 증가 시 collapse 지원

---

## 4. 페이지 라우팅 재설계

### 4.1 현행 라우팅 (AS-IS)

```
src/app/
├── page.tsx                          /                    랜딩 (서버)
├── (auth)/
│   ├── login/page.tsx                /login               로그인 (클라이언트)
│   ├── signup/page.tsx               /signup              회원가입 (클라이언트)
│   └── reset-password/page.tsx       /reset-password      비밀번호 재설정
├── (dashboard)/
│   └── dashboard/page.tsx            /dashboard           프로젝트 목록 (클라이언트)
├── project/[id]/
│   ├── layout.tsx                    프로젝트 공통 레이아웃 (탭 네비게이션)
│   ├── page.tsx                      /project/[id]        개요 (Overview)
│   ├── services/page.tsx             /project/[id]/services      서비스 목록
│   ├── service-map/page.tsx          /project/[id]/service-map   서비스 맵
│   ├── env/page.tsx                  /project/[id]/env           환경변수
│   ├── health/page.tsx               /project/[id]/health        헬스 모니터링
│   ├── audit/page.tsx                /project/[id]/audit         감사 로그
│   └── settings/page.tsx             /project/[id]/settings      설정
├── services/                         /services            서비스 카탈로그 (퍼블릭)
├── pricing/                          /pricing             요금제
├── settings/tokens/                  /settings/tokens     API 토큰
├── my-sites/                         /my-sites            내 사이트
├── oneclick/                         /oneclick            원클릭 배포
├── guides/                           /guides/*            가이드
└── admin/                            /admin/*             관리자
```

### 4.2 신규 라우팅 (TO-BE)

**핵심 변경**: `/project/[id]` 개요가 → **3-Column 대시보드**로 교체.
기존 탭 기반 네비게이션에서 **대시보드 중심 + 서브 패널** 구조로 전환.

```
src/app/
├── page.tsx                          /                     랜딩 (변경 없음)
├── (auth)/                           /login, /signup, ...  (변경 없음)
│
├── (dashboard)/
│   └── dashboard/page.tsx            /dashboard            프로젝트 목록 (변경 없음)
│
├── project/[id]/
│   ├── layout.tsx                    프로젝트 공통 레이아웃 (★ 탭 구조 변경)
│   │
│   │   ── 메인 탭 ──────────────────────────────────────
│   ├── page.tsx                      /project/[id]         ★ 3-Column 대시보드 (신규)
│   ├── service-map/page.tsx          /project/[id]/service-map   서비스 맵 (유지)
│   │
│   │   ── 서브 탭 (대시보드 내 연동) ──────────────────
│   ├── services/page.tsx             /project/[id]/services      서비스 카탈로그 (역할 변경)
│   ├── env/page.tsx                  /project/[id]/env           환경변수 (유지)
│   │
│   │   ── 관리 탭 ──────────────────────────────────────
│   ├── connections/page.tsx          /project/[id]/connections   ★ 연결 관리 (신규)
│   ├── health/page.tsx               /project/[id]/health        헬스 모니터링 (유지)
│   ├── audit/page.tsx                /project/[id]/audit         감사 로그 (유지)
│   └── settings/page.tsx             /project/[id]/settings      설정 (유지)
│
├── services/                         /services             (변경 없음)
├── settings/                         /settings/*           (변경 없음)
├── my-sites/                         /my-sites             (변경 없음)
├── oneclick/                         /oneclick             (변경 없음)
├── guides/                           /guides/*             (변경 없음)
└── admin/                            /admin/*              (변경 없음)
```

### 4.3 탭 네비게이션 변경

**AS-IS (7개 탭 동일 레벨):**
```
개요 | 서비스 맵 | 서비스 목록 | 환경변수 | 상태 모니터링 | 감사 로그 | 설정
```

**TO-BE (3그룹 구분):**
```
┌──────────────────────────────────────────────────────────────────────────┐
│  [🏠 대시보드]  [🗺 서비스 맵]  │  [📦 서비스]  [🔑 환경변수]  │        │
│       메인                      │       서브                    │        │
│                                 │                               │        │
│                        [🔗 연결관리]  [💓 헬스]  [📋 감사로그]  [⚙ 설정] │
│                                          관리                            │
└──────────────────────────────────────────────────────────────────────────┘
```

| 그룹 | 탭 | 역할 변경 내용 |
|------|-----|--------------|
| **메인** | 대시보드 | ★ 기존 개요 → 3-Column 레이어 대시보드로 전면 교체 |
| | 서비스 맵 | 유지 (React Flow 전체 시각화) |
| **서브** | 서비스 | 역할 변경: "서비스 목록 관리" → "서비스 카탈로그 + 추가" |
| | 환경변수 | 유지 (암호화 관리, GitHub Secrets 동기화) |
| **관리** | 연결 관리 | ★ 신규: 서비스 간 연결 관계 CRUD |
| | 헬스 | 유지 (모니터링, 스파크라인) |
| | 감사 로그 | 유지 |
| | 설정 | 유지 (팀, GitHub 연동, 삭제) |

### 4.4 페이지별 상세 변경

#### `/project/[id]` — 3-Column 대시보드 (★ 핵심 변경)

**현재**: 메트릭 카드 3개 + 헬스 상태 + 스마트 제안 + 빠른 액션
**변경 후**: 3-Column 레이어 뷰 (섹션 2.1 다이어그램 참조)

```
데이터 소스:
┌─────────────┐     ┌──────────────────┐     ┌────────────────┐
│ services    │────→│ project_services │────→│ service_accounts│
│ (카탈로그)   │     │ (프로젝트 연결)   │     │ (인증 정보)     │
└─────────────┘     └──────────────────┘     └────────────────┘
      │                     │                        │
      │ dashboard_layer     │ status                 │ connection status
      │ dashboard_subcat    │ (connected/error/...)   │ (active/expired)
      ▼                     ▼                        ▼
┌──────────────────────────────────────────────────────────┐
│              3-Column Dashboard Renderer                  │
│  Frontend 컬럼 │ 내 프로젝트 │ Backend 컬럼 │ 개발환경     │
└──────────────────────────────────────────────────────────┘
```

**필요한 데이터 쿼리:**
1. `services` + `dashboard_layer` → 레이어별 서비스 분류
2. `project_services` → 프로젝트에 추가된 서비스 + 상태
3. `service_accounts` → 연결 계정 정보 (마스킹 표시)
4. `environment_vars` → 환경변수 진행률 (설정된 수 / 필요한 수)
5. `health_checks` → 최신 헬스 상태
6. `user_connections` → 서비스 간 연결선 데이터

#### `/project/[id]/connections` — 연결 관리 (★ 신규)

서비스 간 연결 관계를 명시적으로 관리하는 페이지.

**기능:**
- 연결 추가: "Vercel → Supabase" 같은 서비스 간 관계 생성
- 연결 유형: `api_call`, `auth_provider`, `data_source`, `deploy_target`, `monitoring`
- 연결 상태: 초록/빨강/노랑 (대시보드 연결선과 동기화)
- 연결 메모: 관계 설명 텍스트

**데이터 소스:** `user_connections` 테이블 (기존)

#### `/project/[id]/services` — 역할 변경

**현재**: 서비스 목록 + 체크리스트 + 셋업 위저드
**변경 후**: "서비스 카탈로그" — 대시보드에서 [+ 서비스 추가] 시 열리는 카탈로그 뷰

- 레이어별 필터 (Frontend / Backend / 개발환경)
- 서브카테고리별 그룹핑
- 서비스 검색 + 태그 필터
- 서비스 추가 → `project_services` + 기본 레이어 자동 배정

---

## 5. 데이터베이스 변경 (Migration)

### 5.1 변경 개요

3-Column 대시보드를 지원하기 위한 DB 스키마 변경.
**기존 테이블 확장 + 신규 테이블 1개** 추가.

### 5.2 `services` 테이블 확장

기존 `services` 테이블에 **레이어 분류 컬럼 2개** 추가.

```sql
-- Migration: 027_dashboard_layer.sql

-- 레이어 분류 컬럼 추가
ALTER TABLE services
  ADD COLUMN dashboard_layer TEXT DEFAULT 'backend',
  ADD COLUMN dashboard_subcategory TEXT;

-- 기본 분류 설정: Frontend
UPDATE services SET dashboard_layer = 'frontend', dashboard_subcategory = 'deploy'
  WHERE category = 'deploy';
UPDATE services SET dashboard_layer = 'frontend', dashboard_subcategory = 'analytics'
  WHERE category = 'monitoring' AND slug IN ('posthog');
UPDATE services SET dashboard_layer = 'frontend', dashboard_subcategory = 'social_login'
  WHERE category = 'social_login';
UPDATE services SET dashboard_layer = 'frontend', dashboard_subcategory = 'auth'
  WHERE category = 'auth';

-- 기본 분류 설정: Backend
UPDATE services SET dashboard_layer = 'backend', dashboard_subcategory = 'database'
  WHERE category = 'database';
UPDATE services SET dashboard_layer = 'backend', dashboard_subcategory = 'payment'
  WHERE category = 'payment';
UPDATE services SET dashboard_layer = 'backend', dashboard_subcategory = 'email'
  WHERE category = 'email';
UPDATE services SET dashboard_layer = 'backend', dashboard_subcategory = 'storage'
  WHERE category = 'storage';

-- 기본 분류 설정: 개발환경
UPDATE services SET dashboard_layer = 'devtools', dashboard_subcategory = 'ai'
  WHERE category = 'ai';
UPDATE services SET dashboard_layer = 'devtools', dashboard_subcategory = 'cicd'
  WHERE category = 'cicd';
UPDATE services SET dashboard_layer = 'devtools', dashboard_subcategory = 'monitoring'
  WHERE category = 'monitoring' AND slug NOT IN ('posthog');

-- 인덱스
CREATE INDEX idx_services_dashboard_layer ON services (dashboard_layer);
CREATE INDEX idx_services_dashboard_layer_subcat
  ON services (dashboard_layer, dashboard_subcategory);

COMMENT ON COLUMN services.dashboard_layer IS
  'frontend | backend | devtools — 3-Column 대시보드 배치 레이어';
COMMENT ON COLUMN services.dashboard_subcategory IS
  '레이어 내 서브카테고리 (deploy, database, ai 등)';
```

**새 컬럼:**
| 컬럼 | 타입 | 값 | 용도 |
|------|------|-----|------|
| `dashboard_layer` | TEXT | `frontend`, `backend`, `devtools` | 3-Column 배치 레이어 |
| `dashboard_subcategory` | TEXT | `deploy`, `database`, `ai` 등 | 레이어 내 서브그룹 |

### 5.3 `user_connections` 테이블 확장

기존 테이블에 **연결 유형/상태 컬럼** 추가.

```sql
-- Migration: 028_connection_types.sql

-- 연결 유형 및 상태 컬럼 추가
ALTER TABLE user_connections
  ADD COLUMN connection_status TEXT DEFAULT 'pending'
    CHECK (connection_status IN ('active', 'error', 'pending')),
  ADD COLUMN description TEXT,
  ADD COLUMN last_verified_at TIMESTAMPTZ,
  ADD COLUMN metadata JSONB DEFAULT '{}';

-- connection_type 값 표준화 (기존 컬럼 활용)
COMMENT ON COLUMN user_connections.connection_type IS
  'api_call | auth_provider | data_source | deploy_target | monitoring | custom';
COMMENT ON COLUMN user_connections.connection_status IS
  'active(초록) | error(빨강) | pending(노랑) — 대시보드 연결선 색상과 매핑';

-- 상태별 인덱스
CREATE INDEX idx_user_connections_status
  ON user_connections (project_id, connection_status);
```

**연결 상태 ↔ 대시보드 색상 매핑:**
| connection_status | 대시보드 색상 | 연결선 |
|-------------------|:----------:|--------|
| `active` | 초록 (#22c55e) | 실선 |
| `error` | 빨강 (#ef4444) | 실선 + pulse |
| `pending` | 노랑 (#eab308) | 점선 |

### 5.4 `project_service_overrides` 테이블 (★ 신규)

사용자가 서비스의 기본 레이어 배치를 **오버라이드**할 때 사용.

```sql
-- Migration: 029_service_layer_overrides.sql

CREATE TABLE project_service_overrides (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id UUID NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  service_id UUID NOT NULL REFERENCES services(id) ON DELETE CASCADE,
  custom_layer TEXT NOT NULL
    CHECK (custom_layer IN ('frontend', 'backend', 'devtools')),
  custom_subcategory TEXT,
  display_order INTEGER DEFAULT 0,
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE (project_id, service_id)
);

-- 인덱스
CREATE INDEX idx_pso_project ON project_service_overrides (project_id);

-- RLS
ALTER TABLE project_service_overrides ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users manage own project overrides"
  ON project_service_overrides
  FOR ALL
  USING (
    project_id IN (
      SELECT id FROM projects WHERE user_id = auth.uid()
    )
  )
  WITH CHECK (
    project_id IN (
      SELECT id FROM projects WHERE user_id = auth.uid()
    )
  );

-- 팀 멤버 읽기 권한
CREATE POLICY "Team members can view overrides"
  ON project_service_overrides
  FOR SELECT
  USING (
    project_id IN (
      SELECT p.id FROM projects p
      JOIN team_members tm ON tm.team_id = p.team_id
      WHERE tm.user_id = auth.uid()
    )
  );
```

**사용 시나리오:**
- 기본값: `services.dashboard_layer` 사용 (예: Stripe → backend)
- 오버라이드: 사용자가 Stripe를 frontend로 이동 → `project_service_overrides` 레코드 생성
- 쿼리 우선순위: `COALESCE(override.custom_layer, services.dashboard_layer)`

### 5.5 전체 데이터 흐름

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    3-Column Dashboard 데이터 흐름                        │
└─────────────────────────────────────────────────────────────────────────┘

[1] 서비스 목록 조회
    services
      + dashboard_layer        ← 기본 레이어
      + dashboard_subcategory  ← 서브카테고리
    LEFT JOIN project_service_overrides
      → custom_layer           ← 오버라이드 (있으면 우선)

[2] 프로젝트에 추가된 서비스
    project_services
      + status                 ← not_started | in_progress | connected | error
    INNER JOIN services        ← 서비스 메타 정보

[3] 연결 계정 정보 (카드에 ID 표시)
    service_accounts
      + oauth_provider_user_id ← 연결된 계정 ID (마스킹)
      + status                 ← active | expired | error
      + last_verified_at       ← 최근 확인 시간

[4] 환경변수 진행률 (카드에 n/m 표시)
    environment_vars
      WHERE project_id = ?
      GROUP BY service_id
      → COUNT(*) / required_count

[5] 서비스 간 연결선
    user_connections
      + source_service_id      ← 출발 서비스
      + target_service_id      ← 도착 서비스
      + connection_type        ← 연결 유형
      + connection_status      ← active | error | pending (색상 결정)

[6] 헬스 상태 (카드 상태 dot)
    health_checks
      WHERE project_service_id IN (...)
      ORDER BY checked_at DESC
      LIMIT 1 per service      ← 최신 상태만

[렌더링]
    레이어별 그룹핑 → Frontend / 내 프로젝트 / Backend / 개발환경
    서브카테고리별 정렬 → deploy, database, ai, ...
    연결선 렌더링 → source → target (SVG 오버레이 — 섹션 2.7 참조)
    상태 색상 적용 → project_services.status + connection_status + health
```

### 5.6 ER 다이어그램 (변경 부분)

```
services (기존 + 확장)                project_service_overrides (★ 신규)
┌────────────────────────┐           ┌─────────────────────────────┐
│ id                     │           │ id                          │
│ name, slug, category   │           │ project_id ──→ projects     │
│ ...                    │           │ service_id ──→ services     │
│ + dashboard_layer   ★  │←──────────│ custom_layer                │
│ + dashboard_subcategory★│           │ custom_subcategory          │
└──────────┬─────────────┘           │ display_order               │
           │                         └─────────────────────────────┘
           │ 1:N
           ▼
    project_services (기존)
    ┌─────────────────────────┐
    │ id                      │
    │ project_id ──→ projects │
    │ service_id ──→ services │
    │ status                  │
    └──────────┬──────────────┘
               │ 1:N                    user_connections (기존 + 확장)
               ▼                        ┌──────────────────────────────┐
    service_accounts (기존)              │ id                           │
    ┌─────────────────────────┐         │ project_id ──→ projects      │
    │ id                      │         │ source_service_id ──→ services│
    │ project_id              │         │ target_service_id ──→ services│
    │ service_id              │         │ connection_type               │
    │ status                  │         │ + connection_status  ★        │
    │ oauth_provider_user_id  │         │ + description        ★        │
    │ encrypted_access_token  │         │ + last_verified_at   ★        │
    │ last_verified_at        │         │ + metadata           ★        │
    └─────────────────────────┘         └──────────────────────────────┘
```

### 5.7 API 라우트 변경

| 라우트 | 변경 | 설명 |
|--------|------|------|
| `GET /api/projects/[id]/dashboard` | ★ 신규 | 3-Column 대시보드용 통합 데이터 (6개 소스 JOIN) |
| `GET /api/connections` | 확장 | `connection_status`, `description` 필드 추가 |
| `PATCH /api/connections/[id]` | 확장 | status 업데이트 지원 |
| `POST /api/projects/[id]/layer-override` | ★ 신규 | 서비스 레이어 오버라이드 |
| `DELETE /api/projects/[id]/layer-override/[serviceId]` | ★ 신규 | 오버라이드 제거 (기본값 복원) |

#### 대시보드 통합 API 응답 구조

```typescript
// GET /api/projects/[id]/dashboard
interface DashboardResponse {
  project: {
    id: string;
    name: string;
    description: string;
    metrics: {
      totalServices: number;
      connectedServices: number;
      totalEnvVars: number;
      configuredEnvVars: number;
      progressPercent: number;
    };
  };
  layers: {
    frontend: LayerData;
    backend: LayerData;
    devtools: LayerData;
  };
  connections: ConnectionLine[];
}

interface LayerData {
  subcategories: {
    [key: string]: ServiceCardData[];  // e.g. "deploy": [Vercel, Netlify]
  };
}

interface ServiceCardData {
  serviceId: string;
  name: string;
  slug: string;
  iconUrl: string;
  category: string;
  dashboardLayer: string;         // 실제 배치 레이어 (오버라이드 반영)
  dashboardSubcategory: string;
  status: 'not_started' | 'in_progress' | 'connected' | 'error';
  account: {
    providerId: string | null;     // 마스킹된 계정 ID
    status: string | null;
    lastVerifiedAt: string | null;
  } | null;
  envProgress: {
    configured: number;
    required: number;
  };
  healthStatus: 'healthy' | 'degraded' | 'unhealthy' | 'unknown';
  externalUrl: string | null;
}

interface ConnectionLine {
  id: string;
  sourceServiceId: string;
  targetServiceId: string;
  connectionType: string;
  connectionStatus: 'active' | 'error' | 'pending';
  label: string | null;
}
```

### 5.8 프론트엔드 상태 관리

#### TanStack Query 훅 (신규)

| 훅 | 파일 | 역할 |
|----|------|------|
| `useDashboard(projectId)` | `src/lib/queries/dashboard.ts` | 대시보드 통합 데이터 조회 (6개 소스) |
| `useConnections(projectId)` | `src/lib/queries/connections.ts` | 서비스 간 연결 목록 조회 |
| `useCreateConnection()` | `src/lib/queries/connections.ts` | 연결 생성 mutation |
| `useUpdateConnection()` | `src/lib/queries/connections.ts` | 연결 상태/설명 수정 mutation |
| `useDeleteConnection()` | `src/lib/queries/connections.ts` | 연결 삭제 mutation |
| `useLayerOverride()` | `src/lib/queries/dashboard.ts` | 레이어 오버라이드 mutation |
| `useRemoveLayerOverride()` | `src/lib/queries/dashboard.ts` | 오버라이드 제거 mutation |

**캐싱 전략:**
```typescript
// src/lib/queries/dashboard.ts
export const dashboardKeys = {
  all: (projectId: string) => ['dashboard', projectId] as const,
  connections: (projectId: string) => ['dashboard', projectId, 'connections'] as const,
};

// useDashboard 설정
{
  staleTime: 30_000,       // 30초 — 자주 변경되지 않는 데이터
  refetchOnWindowFocus: true,
  refetchInterval: 60_000, // 1분마다 백그라운드 리프레시
}

// useConnections 설정
{
  staleTime: 10_000,       // 10초 — 연결 상태는 더 자주 갱신
}
```

**Invalidation 관계:**
- 서비스 추가/삭제 → `dashboardKeys.all` invalidate
- 연결 CRUD → `dashboardKeys.connections` invalidate
- 환경변수 변경 → `dashboardKeys.all` invalidate (진행률 갱신)
- 헬스체크 실행 → `dashboardKeys.all` invalidate (상태 갱신)

#### Zustand 스토어 (신규)

```typescript
// src/stores/dashboard-store.ts
interface DashboardStore {
  // 모바일 탭
  activeTab: 'frontend' | 'myProject' | 'backend' | 'devtools';
  setActiveTab: (tab: DashboardStore['activeTab']) => void;

  // 확장된 카드
  expandedCardId: string | null;
  toggleCard: (cardId: string) => void;

  // 연결선 표시
  showConnections: boolean;
  toggleConnections: () => void;

  // 온보딩
  onboardingComplete: boolean;
  completeOnboarding: () => void;

  // 연결 추가 다이얼로그
  connectionDialogOpen: boolean;
  connectionDialogSource: string | null;
  openConnectionDialog: (sourceServiceId?: string) => void;
  closeConnectionDialog: () => void;
}
```

---

### 5.9 서비스 맵 ↔ 대시보드 동기화

서비스 맵(`/project/[id]/service-map`)과 3-Column 대시보드는 **같은 데이터 소스**를 공유한다.

```
                    ┌─────────────────────┐
                    │  user_connections    │ ← 단일 데이터 소스
                    │  project_services   │
                    └──────────┬──────────┘
                               │
                    ┌──────────┴──────────┐
                    │                     │
              ┌─────▼─────┐        ┌─────▼──────┐
              │ 3-Column  │        │ Service Map │
              │ Dashboard │        │ (React Flow)│
              │ (SVG 연결) │        │ (노드+엣지) │
              └───────────┘        └────────────┘
```

**동기화 규칙:**
| 동작 위치 | 변경 내용 | 반영 대상 | 방법 |
|----------|----------|----------|------|
| 대시보드에서 연결 추가 | `user_connections` INSERT | 서비스 맵에 엣지 추가 | QueryKey invalidate |
| 서비스 맵에서 엣지 추가 | `user_connections` INSERT | 대시보드에 연결선 추가 | QueryKey invalidate |
| 대시보드에서 서비스 추가 | `project_services` INSERT | 서비스 맵에 노드 추가 | QueryKey invalidate |
| 서비스 맵에서 노드 삭제 | `project_services` DELETE | 대시보드에서 카드 제거 | QueryKey invalidate |
| 연결 관리 탭에서 수정 | `user_connections` UPDATE | 양쪽 모두 반영 | QueryKey invalidate |

**충돌 방지:**
- 낙관적 업데이트(optimistic update)는 사용하지 않음 — 서버 응답 후 반영
- 동시 편집 시 마지막 쓰기 우선 (Last Write Wins)
- `refetchOnWindowFocus: true`로 탭 전환 시 자동 동기화

---

### 5.10 레이어 오버라이드 UI

사용자가 서비스의 레이어 배치를 변경하는 인터랙션.

#### 방법: 컨텍스트 메뉴

컴팩트 카드의 **⋮ 메뉴**에 "레이어 이동" 옵션을 추가.

```
⋮ 메뉴 (DropdownMenu)
├── ⚙ 설정
├── 📄 문서 열기
├── 🔗 연결 해제
├── ──────────────
├── 📂 레이어 이동 →  ┌──────────────────┐
│                     │ ○ Frontend        │
│                     │ ● Backend (현재)   │
│                     │ ○ 개발환경         │
│                     │ ──────────────────│
│                     │ ↩ 기본값 복원      │
│                     └──────────────────┘
└── 🗑 서비스 제거
```

**동작:**
1. 레이어 선택 → `POST /api/projects/[id]/layer-override` 호출
2. `project_service_overrides` 레코드 생성/업데이트
3. 대시보드 QueryKey invalidate → 카드가 새 레이어로 이동
4. "기본값 복원" → `DELETE /api/projects/[id]/layer-override/[serviceId]`

**시각적 표시:**
- 오버라이드된 서비스 카드에 작은 `↔` 아이콘 표시 (기본 위치가 아님을 알림)
- 툴팁: "원래 위치: Backend — 사용자가 Frontend로 이동함"

---

## 6. 제약 조건 및 고려사항

### 6.1 기술 제약
- Next.js 16 App Router 기반 유지
- Supabase + RLS 인증/인가 구조 유지
- 기존 DB 스키마 호환성 (ALTER TABLE + 신규 테이블, 기존 데이터 영향 없음)

### 6.2 UX 원칙
- Korean-first, 글로벌 확장 고려
- 모바일 반응형 필수
- 다크모드 지원

### 6.3 기존 자산 활용
- React Flow 서비스 맵 (서비스 맵 페이지 유지)
- TanStack Query 데이터 패칭 (대시보드 통합 API용 훅 추가)
- Zustand 상태 관리 (대시보드 UI 상태)
- shadcn/ui 컴포넌트

### 6.4 마이그레이션 전략
- `027_dashboard_layer.sql`: services 테이블에 레이어 컬럼 추가 + 기존 데이터 분류
- `028_connection_types.sql`: user_connections 확장
- `029_service_layer_overrides.sql`: 오버라이드 테이블 생성
- 모든 마이그레이션은 **비파괴적** (ALTER ADD, CREATE TABLE — 기존 기능 영향 없음)

### 6.5 성능 / 캐싱 전략

**대시보드 통합 API 최적화:**

| 항목 | 전략 |
|------|------|
| DB 쿼리 | 6개 소스를 **병렬 Promise.all**로 조회, 개별 쿼리 최적화 |
| 인덱스 | `dashboard_layer`, `project_id`, `connection_status` 복합 인덱스 활용 |
| 페이지네이션 | 서비스 카드: 불필요 (프로젝트당 최대 30~50개). 감사 로그만 페이지네이션 유지 |
| 응답 크기 | 서비스당 ~200바이트 × 50개 = ~10KB (경량) |
| 클라이언트 캐싱 | TanStack Query staleTime 30초, 백그라운드 리프레시 60초 |
| SVG 연결선 | `ResizeObserver` + `requestAnimationFrame`으로 리페인트 최소화 |
| 대량 서비스 | 레이어별 접기/펼치기로 DOM 노드 최소화 (collapsed 시 렌더링 안 함) |

**Quota 연동:**
- `plan_quotas.max_services_per_proj`에 따라 서비스 추가 제한
- Free: 10개, Pro: 50개, Team: 무제한

---

## 7. 구현 우선순위 (Phase)

### Phase 1: 핵심 대시보드 (MVP)
> 목표: 3-Column 레이아웃 + 서비스 카드 기본 동작

| 항목 | 내용 |
|------|------|
| DB | Migration 027 (dashboard_layer) 적용 |
| API | `GET /api/projects/[id]/dashboard` 구현 |
| UI | 3-Column 레이아웃 + 컴팩트 카드 + 내 프로젝트 카드 |
| 상태 | 기존 `project_services.status` 기반 3색 표시 |
| 탭 | ProjectTabs 3그룹 구조로 변경 |
| 반응형 | 데스크톱 3단 + 모바일 탭 전환 |

**이 단계에서 제외:**
- 연결선, 확장 카드, 온보딩, 오버라이드, 자동 연결

### Phase 2: 연결선 & 확장 카드
> 목표: 서비스 간 관계 시각화 + 카드 상세 정보

| 항목 | 내용 |
|------|------|
| DB | Migration 028 (connection_types) 적용 |
| API | `GET/POST/PATCH/DELETE /api/connections` 확장 |
| UI | SVG 연결선 렌더링 + 확장 카드 (클릭 펼침) |
| 연결 | 자동 연결 (service_dependencies 기반) + 수동 연결 추가 UI |
| 동기화 | 서비스 맵 ↔ 대시보드 QueryKey 연동 |
| 페이지 | `/project/[id]/connections` 신규 페이지 |

### Phase 3: 사용자 커스터마이징
> 목표: 레이어 오버라이드 + 온보딩 + 퀵액션

| 항목 | 내용 |
|------|------|
| DB | Migration 029 (service_layer_overrides) 적용 |
| API | 오버라이드 CRUD API |
| UI | ⋮ 메뉴 → 레이어 이동 + 오버라이드 표시 |
| 온보딩 | 빈 상태 UI + 첫 진입 가이드 + 프리셋 추천 |
| 퀵액션 | 확장 카드 내 복사/문서/설정 버튼 |
| i18n | 대시보드 라벨 한/영 번역 적용 |

### Phase 4: 고도화
> 목표: 성능 최적화 + 확장 기능 + 신규 서비스

| 항목 | 내용 |
|------|------|
| 성능 | SVG 연결선 최적화, collapsed 레이어 가상화 |
| 프리셋 | 서비스 프리셋/템플릿 원클릭 추가 |
| 신규 서비스 | 27개 신규 서비스 데이터 추가 (seed) |
| 인증 영역 | Frontend 내 점선 박스 그룹 UI |
| 개발환경 | 하단 레이어 미니 카드 + 서브카테고리 그룹핑 |
| 접근성 | 키보드 네비게이션, 스크린 리더 대응 |

### Phase 요약

```
Phase 1 (MVP)          Phase 2 (연결)        Phase 3 (커스텀)      Phase 4 (고도화)
┌────────────┐        ┌────────────┐        ┌────────────┐        ┌────────────┐
│ 3-Column   │   →    │ SVG 연결선  │   →    │ 레이어 이동  │   →    │ 성능 최적화  │
│ 레이아웃    │        │ 확장 카드   │        │ 온보딩      │        │ 프리셋      │
│ 컴팩트 카드  │        │ 자동+수동   │        │ 퀵액션      │        │ 신규 서비스  │
│ 모바일 탭   │        │ 연결 관리   │        │ i18n        │        │ 접근성      │
│ Migration 027│       │ Migration 028│      │ Migration 029│       │ 데이터 시드  │
└────────────┘        └────────────┘        └────────────┘        └────────────┘
```

---

## 8. 변경 이력

| 날짜 | 내용 | 비고 |
|------|------|------|
| 2026-02-18 | 초안 작성, 현행 구조 정리 | AS-IS 분석 완료 |
| 2026-02-18 | 컨셉 확정: 3-Column + 개발환경 레이어 | 7개 결정사항 반영 |
| 2026-02-18 | 서비스 상세 매핑: 54개 서비스 4레이어 분류 | 서브카테고리 포함 |
| 2026-02-18 | 인증 영역을 Frontend로 이동 + 점선 구분 | Clerk, NextAuth, 소셜로그인 통합 |
| 2026-02-18 | 서비스 카드 UI 와이어프레임 상세화 | 7종 카드 + 인터랙션 + 재활용 계획 |
| 2026-02-18 | 라우팅 재설계 + DB 변경 + 연결 구조 | 3 migration + 통합 API + 탭 재구성 |
| 2026-02-18 | 전체 검토: 12개 누락 항목 추가 | 빈상태, 연결선, 성능, 다크모드, i18n 등 |
| 2026-02-18 | 구현 우선순위 4-Phase 정의 | MVP → 연결 → 커스텀 → 고도화 |
| 2026-02-18 | Quick Start 섹션 추가 + 문서 최종 정리 | 신규 세션 컨텍스트 연속성 확보 |
